"use client";

// BONUS-05 deferred per D-08 — stub only; no OAuth wiring

export function SignInButton() {
  return (
    <button
      type="button"
      className="bg-pen text-white h-12 w-full uppercase tracking-widest text-xs font-[family-name:var(--font-body)]"
    >
      SIGN IN WITH GOOGLE
    </button>
  );
}
