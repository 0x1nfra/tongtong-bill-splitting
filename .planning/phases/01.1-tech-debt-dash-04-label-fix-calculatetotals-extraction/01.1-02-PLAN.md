---
phase: "01.1"
plan: 2
type: execute
wave: 2
depends_on:
  - "01.1-01"
files_modified:
  - src/app/dashboard/[billId]/page.tsx
  - src/components/BillSummaryCard.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "dashboard/[billId]/page.tsx imports calculateTotals from @/lib/calculateTotals — inline function removed"
    - "BillSummaryCard.tsx imports calculateTotals from @/lib/calculateTotals — inline function removed"
    - "BillSummaryCard destructures grandTotalCents (not grandTotal) from the shared utility"
    - "Dashboard page void rejected suppressor is preserved after refactor"
    - "TypeScript strict mode passes after both changes"
  artifacts:
    - path: "src/app/dashboard/[billId]/page.tsx"
      provides: "Dashboard page using shared calculateTotals utility"
      contains: "import { calculateTotals } from \"@/lib/calculateTotals\""
    - path: "src/components/BillSummaryCard.tsx"
      provides: "BillSummaryCard using shared calculateTotals utility"
      contains: "import { calculateTotals } from \"@/lib/calculateTotals\""
  key_links:
    - from: "src/lib/calculateTotals.ts"
      to: "src/app/dashboard/[billId]/page.tsx"
      via: "named import"
      pattern: "import.*calculateTotals.*@/lib/calculateTotals"
    - from: "src/lib/calculateTotals.ts"
      to: "src/components/BillSummaryCard.tsx"
      via: "named import"
      pattern: "import.*calculateTotals.*@/lib/calculateTotals"
---

<objective>
Replace the inline calculateTotals functions in src/app/dashboard/[billId]/page.tsx and src/components/BillSummaryCard.tsx with imports from the shared utility created in Plan 01.

Purpose: Both files are single-field consumers (only grandTotalCents is used from the return value). The changes are structurally identical: delete the inline function block, add the import, verify the destructure uses the canonical field names. Grouping them in one plan minimizes context overhead.

Output: Two modified files — both now import from @/lib/calculateTotals instead of defining their own inline function.
</objective>

<execution_context>
@$HOME/.claire/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-tech-debt-dash-04-label-fix-calculatetotals-extraction/01.1-CONTEXT.md
@.planning/phases/01.1-tech-debt-dash-04-label-fix-calculatetotals-extraction/01.1-RESEARCH.md
@.planning/phases/01.1-tech-debt-dash-04-label-fix-calculatetotals-extraction/01.1-01-SUMMARY.md

<interfaces>
From src/lib/calculateTotals.ts (created in Plan 01):
  export function calculateTotals(
    items: Array<{ price: number; quantity: number }>,
    applySST: boolean,
    applyServiceCharge: boolean
  ): {
    subtotalCents: number;
    serviceChargeCents: number;
    sstCents: number;
    grandTotalCents: number;
  }

Import path (both consumers):
  import { calculateTotals } from "@/lib/calculateTotals";
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline calculateTotals in dashboard/[billId]/page.tsx</name>
  <files>src/app/dashboard/[billId]/page.tsx</files>
  <read_first>
    - src/app/dashboard/[billId]/page.tsx — read the full file before touching anything; confirm the inline function block (lines 12–30), the import block (lines 1–10), the callsite (lines 125–129), and the DASH-04 section (lines 136–141)
    - src/lib/calculateTotals.ts — confirm the file exists and exports calculateTotals (created in Plan 01)
    - .planning/phases/01.1-tech-debt-dash-04-label-fix-calculatetotals-extraction/01.1-PATTERNS.md — Pattern assignment for dashboard page
  </read_first>
  <action>
    Make two edits to src/app/dashboard/[billId]/page.tsx:

    EDIT 1 — Add import:
    In the import block at the top of the file (after the existing "use client" directive and before or after the existing named imports), add:
      import { calculateTotals } from "@/lib/calculateTotals";
    Position it after the React/Convex/component imports, consistent with project convention of grouping local utility imports last.

    EDIT 2 — Remove inline function:
    Delete the entire inline calculateTotals function block. This is the JSDoc comment plus the function declaration, which currently spans from the JSDoc line ("/** calculateTotals — derives grand total...") through the closing brace of the function body (the line containing `return { subtotal, serviceCharge, sst, grandTotalCents };`). The block is approximately lines 12–30 in the current file.

    Do NOT touch:
    - The callsite at lines 125–129: `const { grandTotalCents } = calculateTotals(items, bill.applySST, bill.applyServiceCharge);` — this destructure already uses the canonical field name grandTotalCents and requires no change
    - Lines 136–141 (DASH-04 section): the rejected variable, void rejected suppressor, and unclaimed = 0 must remain exactly as they are
    - Any other code in the file

    After edits, the file must have:
    - `import { calculateTotals } from "@/lib/calculateTotals";` in the import block
    - No inline `function calculateTotals` definition anywhere in the file
    - `const { grandTotalCents } = calculateTotals(...)` callsite unchanged at its original position
    - `void rejected;` suppressor line still present
  </action>
  <verify>
    <automated>cd /Users/irfanmurad/Developer/vessl/tongtong-bill-splitting && grep -n "import.*calculateTotals\|function calculateTotals\|grandTotalCents\|void rejected" src/app/dashboard/\[billId\]/page.tsx && pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep for "import.*calculateTotals.*@/lib/calculateTotals" in the file returns 1 match
    - grep for "function calculateTotals" in the file returns 0 matches (inline function deleted)
    - grep for "grandTotalCents" in the file returns at least 1 match (callsite still present)
    - grep for "void rejected" in the file returns 1 match (suppressor preserved)
    - pnpm tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Dashboard page imports calculateTotals from @/lib/calculateTotals, inline function removed, grandTotalCents callsite intact, void rejected suppressor preserved, TypeScript passes.</done>
</task>

<task type="auto">
  <name>Task 2: Replace inline calculateTotals in BillSummaryCard.tsx</name>
  <files>src/components/BillSummaryCard.tsx</files>
  <read_first>
    - src/components/BillSummaryCard.tsx — read the full file before touching anything; confirm the inline function block (lines 15–35), the callsite at line 44 (destructures grandTotal), and the display site at line 70 (grandTotal / 100).toFixed(2)
    - src/lib/calculateTotals.ts — confirm the file exists and exports calculateTotals
    - .planning/phases/01.1-tech-debt-dash-04-label-fix-calculatetotals-extraction/01.1-PATTERNS.md — BillSummaryCard pattern assignment; note the grandTotal → grandTotalCents rename requirement
  </read_first>
  <action>
    Make three edits to src/components/BillSummaryCard.tsx:

    EDIT 1 — Add import:
    After the "use client"; directive (which is the first line), add as the very next line:
      import { calculateTotals } from "@/lib/calculateTotals";
    This file currently has no imports — the import is the first statement after the directive.

    EDIT 2 — Remove inline function:
    Delete the entire inline calculateTotals function block. The block begins with the JSDoc comment ("/** calculateTotals — derives grand total...") and ends with the closing brace of the function body (`return { subtotal, serviceCharge, sst, grandTotal };`). Currently approximately lines 11–35.

    EDIT 3 — Rename destructure and display-site variable:
    At the callsite (currently: `const { grandTotal } = calculateTotals(items, applySST, applyServiceCharge);`), rename the destructured field from `grandTotal` to `grandTotalCents`:
      const { grandTotalCents } = calculateTotals(items, applySST, applyServiceCharge);

    At the display site (currently: `RM{(grandTotal / 100).toFixed(2)}`), update the variable name only — the arithmetic is unchanged:
      RM{(grandTotalCents / 100).toFixed(2)}

    Why the rename: The shared utility returns grandTotalCents (with Cents suffix per D-03). The old inline function returned grandTotal (short name). TypeScript strict mode will error on `Property 'grandTotal' does not exist` if the destructure is not updated.

    Do NOT touch:
    - The BillSummaryCardProps type definition
    - The BillSummaryCard component function signature
    - Any JSX other than the one display site at line 70
  </action>
  <verify>
    <automated>cd /Users/irfanmurad/Developer/vessl/tongtong-bill-splitting && grep -n "import.*calculateTotals\|function calculateTotals\|grandTotal\|grandTotalCents" src/components/BillSummaryCard.tsx && pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep for "import.*calculateTotals.*@/lib/calculateTotals" in the file returns 1 match
    - grep for "function calculateTotals" in the file returns 0 matches (inline function deleted)
    - grep for "grandTotalCents" in the file returns 2 matches (one at destructure, one at display site)
    - grep for "grandTotal[^C]" in the file returns 0 matches — no plain `grandTotal` usage remains (the old short-name is fully replaced)
    - pnpm tsc --noEmit exits 0
  </acceptance_criteria>
  <done>BillSummaryCard imports calculateTotals from @/lib/calculateTotals, inline function removed, grandTotal renamed to grandTotalCents at destructure and display site, TypeScript passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new boundaries | This plan modifies two client components to use a shared utility. No new external I/O or trust boundaries introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-02 | Tampering | Arithmetic correctness during refactor | accept | The shared utility arithmetic was verified identical to the source inline variants in RESEARCH.md; TypeScript strict mode will catch field-name mismatches at compile time |
| T-01.1-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan |
</threat_model>

<verification>
After both tasks complete:
1. grep for "import { calculateTotals } from" in src/app/dashboard/[billId]/page.tsx returns 1 match
2. grep for "import { calculateTotals } from" in src/components/BillSummaryCard.tsx returns 1 match
3. grep for "function calculateTotals" across both files returns 0 matches
4. grep for "void rejected" in dashboard page returns 1 match (suppressor preserved)
5. pnpm tsc --noEmit exits 0 across the full project
</verification>

<success_criteria>
- dashboard/[billId]/page.tsx and BillSummaryCard.tsx each import from @/lib/calculateTotals
- Both inline calculateTotals functions deleted
- BillSummaryCard destructure and display site updated from grandTotal to grandTotalCents
- Dashboard void rejected suppressor untouched
- pnpm tsc --noEmit exits 0
</success_criteria>

<output>
Create `.planning/phases/01.1-tech-debt-dash-04-label-fix-calculatetotals-extraction/01.1-02-SUMMARY.md` when done.
</output>
