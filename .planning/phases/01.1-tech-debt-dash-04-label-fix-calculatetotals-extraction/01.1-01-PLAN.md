---
phase: "01.1"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/calculateTotals.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "src/lib/calculateTotals.ts exists and exports calculateTotals as a named export"
    - "calculateTotals accepts (items: Array<{ price: number; quantity: number }>, applySST: boolean, applyServiceCharge: boolean) and returns { subtotalCents, serviceChargeCents, sstCents, grandTotalCents } — all numbers in integer RM cents"
    - "DASH-04 fix is confirmed present in dashboard page: rejected variable correctly named, void rejected suppressor preserved, StatsBar receives claimed={0}"
    - "src/test/calculateTotals.test.ts is untouched (D-02)"
    - "src/components/RunningTotal.tsx is untouched (D-01)"
  artifacts:
    - path: "src/lib/calculateTotals.ts"
      provides: "Shared calculateTotals utility for Convex-items consumers"
      exports: ["calculateTotals"]
  key_links:
    - from: "src/lib/calculateTotals.ts"
      to: "src/app/c/[billId]/page.tsx"
      via: "import { calculateTotals } from '@/lib/calculateTotals'"
      pattern: "calculateTotals"
    - from: "src/lib/calculateTotals.ts"
      to: "src/app/dashboard/[billId]/page.tsx"
      via: "import { calculateTotals } from '@/lib/calculateTotals'"
      pattern: "calculateTotals"
    - from: "src/lib/calculateTotals.ts"
      to: "src/components/BillSummaryCard.tsx"
      via: "import { calculateTotals } from '@/lib/calculateTotals'"
      pattern: "calculateTotals"
---

<objective>
Create the shared calculateTotals utility at src/lib/calculateTotals.ts, and confirm the DASH-04 fix is already in place in the dashboard page.

Purpose: This utility eliminates the three inline copies of calculateTotals duplicated across dashboard, member view, and BillSummaryCard. Plans 02 and 03 (Wave 2) replace those inline copies with imports from this file. DASH-04 was fixed in a prior commit — this plan confirms the state before Wave 2 proceeds.

Output: src/lib/calculateTotals.ts (new file), confirmation that dashboard DASH-04 state matches research assertions (no code change required there).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-tech-debt-dash-04-label-fix-calculatetotals-extraction/01.1-CONTEXT.md
@.planning/phases/01.1-tech-debt-dash-04-label-fix-calculatetotals-extraction/01.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/lib/calculateTotals.ts</name>
  <files>src/lib/calculateTotals.ts</files>
  <read_first>
    - src/app/c/[billId]/page.tsx — source of canonical arithmetic (lines 15–41); the c-page variant is the most complete and uses the correct Cents-suffix naming
    - .planning/phases/01.1-tech-debt-dash-04-label-fix-calculatetotals-extraction/01.1-CONTEXT.md — D-01 through D-04 (locked decisions on scope, return shape, and file location)
    - .planning/phases/01.1-tech-debt-dash-04-label-fix-calculatetotals-extraction/01.1-RESEARCH.md — Pattern 1: Canonical Utility Module
  </read_first>
  <action>
    Create src/lib/calculateTotals.ts as a pure TypeScript module with no React dependency and no "use client" directive. The file must export exactly one named function: calculateTotals.

    Function signature (per D-03, D-04):
      export function calculateTotals(
        items: Array<{ price: number; quantity: number }>,
        applySST: boolean,
        applyServiceCharge: boolean
      ): {
        subtotalCents: number;
        serviceChargeCents: number;
        sstCents: number;
        grandTotalCents: number;
      }

    Implementation logic (per D-03 — cents only, no pre-formatted strings):
    - subtotalCents = items.reduce((sum, item) => sum + item.price * item.quantity, 0)
    - serviceChargeCents = applyServiceCharge ? Math.round(subtotalCents * 0.1) : 0
    - afterServiceCharge = subtotalCents + serviceChargeCents
    - sstCents = applySST ? Math.round(afterServiceCharge * 0.06) : 0
    - grandTotalCents = afterServiceCharge + sstCents
    - return { subtotalCents, serviceChargeCents, sstCents, grandTotalCents }

    Critical constraints:
    - No Math.round on subtotalCents — it is a sum of integer products (already integer)
    - Math.round is required on the percentage calculations (* 0.1 and * 0.06) to avoid floating-point drift
    - No const enum or namespace-only imports — isolatedModules: true is in effect
    - No relative imports — this file has zero imports (pure TypeScript, no React, no Convex)
    - Include JSDoc comment: "calculateTotals — derives totals from Convex items stored as integer RM cents. Service charge (10%) applied before SST (6%) — Malaysian restaurant convention. Returns all values in integer RM cents. Callers format with (v / 100).toFixed(2)."
  </action>
  <verify>
    <automated>cd /Users/irfanmurad/Developer/vessl/tongtong-bill-splitting && pnpm tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/calculateTotals.ts exists
    - File exports `calculateTotals` as a named export (grep -c "export function calculateTotals" src/lib/calculateTotals.ts returns 1)
    - File contains no "use client" directive
    - File contains no import statements (pure utility — zero dependencies)
    - Function return type annotation is explicit: includes subtotalCents, serviceChargeCents, sstCents, grandTotalCents all typed as number
    - pnpm tsc --noEmit exits 0 (no TypeScript errors introduced)
  </acceptance_criteria>
  <done>src/lib/calculateTotals.ts created, exports calculateTotals with the canonical cents-only return shape, TypeScript strict mode passes.</done>
</task>

<task type="auto">
  <name>Task 2: Verify DASH-04 fix is already present in dashboard page</name>
  <files></files>
  <read_first>
    - src/app/dashboard/[billId]/page.tsx — read the full file; confirm lines 136–141 and 203–208 match the expected fixed state
    - .planning/phases/01.1-tech-debt-dash-04-label-fix-calculatetotals-extraction/01.1-CONTEXT.md — D-05, D-06 (claimed=0 hardcoded, rejected variable correctly named)
    - .planning/phases/01-working-bill/01-REVIEW.md — CR-04 original finding for reference
  </read_first>
  <action>
    Read src/app/dashboard/[billId]/page.tsx and assert the following conditions are true. Do NOT modify the file.

    Assert lines around 136–141 contain exactly this pattern (variable names, filter predicates, void suppressor):
      const confirmed = payments?.filter((p) => p.status === "settled").length ?? 0;
      const awaiting  = payments?.filter((p) => p.status === "pending").length ?? 0;
      const rejected  = payments?.filter((p) => p.status === "rejected").length ?? 0;
      const unclaimed = 0;
      void rejected;

    Assert the StatsBar usage (lines around 203–208) passes claimed={0} — NOT claimed={rejected} or claimed={confirmed}.

    If ALL assertions pass: log "DASH-04 fix confirmed present — no code change required" and mark task done.

    If ANY assertion fails (e.g., the variable is still named `claimed` instead of `rejected`, or StatsBar receives claimed={rejected}): apply the fix inline as follows:
      - Rename the computed variable from `claimed` to `rejected` in the payment filter chain
      - Add `void rejected;` after the unclaimed line to suppress the TypeScript unused-variable error
      - Change `<StatsBar claimed={rejected}` to `<StatsBar claimed={0}`
      - This is the per-D-05 fix: claimed=0 is honest for Phase 1 (Phase 2 will wire claims table data)
  </action>
  <verify>
    <automated>cd /Users/irfanmurad/Developer/vessl/tongtong-bill-splitting && grep -n "void rejected\|claimed={0}\|const rejected" src/app/dashboard/\[billId\]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep for "void rejected" in src/app/dashboard/[billId]/page.tsx returns at least 1 match
    - grep for "claimed={0}" in src/app/dashboard/[billId]/page.tsx returns at least 1 match
    - grep for "const rejected" in src/app/dashboard/[billId]/page.tsx returns at least 1 match
    - grep for "claimed={rejected}" in src/app/dashboard/[billId]/page.tsx returns 0 matches (the bug is not present)
    - pnpm tsc --noEmit exits 0
  </acceptance_criteria>
  <done>DASH-04 fix confirmed in dashboard page: rejected variable correctly named, void rejected suppressor preserved, StatsBar claimed prop hardcoded to 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new boundaries | This plan creates a pure utility module and reads existing source files. No new I/O, no API calls, no external data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-01 | Tampering | calculateTotals arithmetic | accept | Function is pure with no I/O; inputs come from Convex-validated integer cents stored server-side; no user string parsing in this utility |
| T-01.1-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this phase; slopcheck not required |
</threat_model>

<verification>
After both tasks complete:
1. `src/lib/calculateTotals.ts` exists and exports `calculateTotals`
2. `pnpm tsc --noEmit` exits 0
3. Dashboard page has `void rejected` and `claimed={0}` in StatsBar
4. `src/test/calculateTotals.test.ts` is unchanged (verify with `git diff src/test/`)
5. `src/components/RunningTotal.tsx` is unchanged (verify with `git diff src/components/RunningTotal.tsx`)
</verification>

<success_criteria>
- src/lib/calculateTotals.ts created with canonical cents-only return shape (D-03, D-04)
- DASH-04 fix confirmed present (D-05) — no regression
- TypeScript strict mode passes (pnpm tsc --noEmit exits 0)
- No changes to RunningTotal.tsx or src/test/calculateTotals.test.ts
</success_criteria>

<output>
Create `.planning/phases/01.1-tech-debt-dash-04-label-fix-calculatetotals-extraction/01.1-01-SUMMARY.md` when done.
</output>
