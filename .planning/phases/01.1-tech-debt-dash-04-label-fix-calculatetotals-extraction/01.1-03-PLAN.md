---
phase: "01.1"
plan: 3
type: execute
wave: 2
depends_on:
  - "01.1-01"
files_modified:
  - src/app/c/[billId]/page.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "c/[billId]/page.tsx imports calculateTotals from @/lib/calculateTotals — inline function removed"
    - "All four JSX display sites updated: totals.subtotal → (totals.subtotalCents / 100).toFixed(2), totals.serviceCharge → (totals.serviceChargeCents / 100).toFixed(2), totals.sst → (totals.sstCents / 100).toFixed(2), totals.grandTotal → (totals.grandTotalCents / 100).toFixed(2)"
    - "The totals object assignment at line 157 is preserved: const totals = calculateTotals(...) — only the inline function above it is deleted"
    - "TypeScript strict mode passes after the change"
  artifacts:
    - path: "src/app/c/[billId]/page.tsx"
      provides: "Member view page using shared calculateTotals utility with updated display sites"
      contains: "import { calculateTotals } from \"@/lib/calculateTotals\""
  key_links:
    - from: "src/lib/calculateTotals.ts"
      to: "src/app/c/[billId]/page.tsx"
      via: "named import"
      pattern: "import.*calculateTotals.*@/lib/calculateTotals"
    - from: "totals object"
      to: "JSX display"
      via: "totals.subtotalCents / 100).toFixed(2)"
      pattern: "totals\\.subtotalCents"
---

<objective>
Replace the inline calculateTotals function in src/app/c/[billId]/page.tsx with an import from the shared utility, and update all four JSX display-site references to use the new cents-only field names.

Purpose: The member view is the most complex consumer — its inline function returns 8 fields (4 cents + 4 pre-formatted strings), and it uses all four display fields (totals.subtotal, totals.serviceCharge, totals.sst, totals.grandTotal) in JSX. After extraction, the shared utility returns only 4 cents fields, so every display site must be updated from `totals.subtotal` to `(totals.subtotalCents / 100).toFixed(2)`, etc.

Output: src/app/c/[billId]/page.tsx modified — inline function removed, import added, four display sites updated to inline toFixed(2) formatting.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-tech-debt-dash-04-label-fix-calculatetotals-extraction/01.1-CONTEXT.md
@.planning/phases/01.1-tech-debt-dash-04-label-fix-calculatetotals-extraction/01.1-RESEARCH.md
@.planning/phases/01.1-tech-debt-dash-04-label-fix-calculatetotals-extraction/01.1-01-SUMMARY.md

<interfaces>
From src/lib/calculateTotals.ts (created in Plan 01):
  export function calculateTotals(
    items: Array<{ price: number; quantity: number }>,
    applySST: boolean,
    applyServiceCharge: boolean
  ): {
    subtotalCents: number;
    serviceChargeCents: number;
    sstCents: number;
    grandTotalCents: number;
  }

Import path:
  import { calculateTotals } from "@/lib/calculateTotals";

Current display-site references in c/[billId]/page.tsx (to be updated):
  totals.subtotal      → (totals.subtotalCents / 100).toFixed(2)
  totals.serviceCharge → (totals.serviceChargeCents / 100).toFixed(2)
  totals.sst           → (totals.sstCents / 100).toFixed(2)
  totals.grandTotal    → (totals.grandTotalCents / 100).toFixed(2)

The totals assignment callsite (must NOT change):
  const totals = calculateTotals(bill.items ?? [], bill.applySST, bill.applyServiceCharge);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline calculateTotals and update all display sites in c/[billId]/page.tsx</name>
  <files>src/app/c/[billId]/page.tsx</files>
  <read_first>
    - src/app/c/[billId]/page.tsx — read the full file before touching anything; locate: import block (lines 1–8), inline calculateTotals function (lines 10–41), totals assignment callsite (line 157), and all four display sites (lines 233, 240, 248, 255 — confirmed by RESEARCH.md)
    - src/lib/calculateTotals.ts — confirm the file exists and exports calculateTotals (must be created in Plan 01 before this task runs)
    - .planning/phases/01.1-tech-debt-dash-04-label-fix-calculatetotals-extraction/01.1-PATTERNS.md — member view pattern assignment, specifically the display-site mapping table
    - .planning/phases/01.1-tech-debt-dash-04-label-fix-calculatetotals-extraction/01.1-RESEARCH.md — "Member View Consumer" section confirming all four display references
  </read_first>
  <action>
    Make three edits to src/app/c/[billId]/page.tsx:

    EDIT 1 — Add import:
    In the import block at the top (lines 1–8), add after the existing imports:
      import { calculateTotals } from "@/lib/calculateTotals";
    The existing Convex imports use relative paths (e.g., "../../../../convex/_generated/api") — leave those unchanged. The new utility import uses the @/ alias.

    EDIT 2 — Remove inline function:
    Delete the entire inline calculateTotals function block. The block starts with the JSDoc comment ("/** calculateTotals — client-side bill total calculation...") and ends at the closing brace of the function body (the `}` that closes `function calculateTotals`). This is currently lines 10–41. The block to delete is the JSDoc + function signature + all function body lines up to and including the return statement and closing brace.

    Do NOT delete: the blank line or any code after the function block. The `useMemberSession` function (lines 48–60) must remain.

    EDIT 3 — Update four display sites:
    The callsite at line 157 (`const totals = calculateTotals(...)`) stays exactly as-is — the variable name `totals` is preserved, only the inline function above it is deleted.

    However, the four JSX display sites must be updated because the shared utility no longer returns pre-formatted string fields. Locate and update each of the following (exact JSX lines; line numbers may shift after Edit 2):

      BEFORE: RM{totals.subtotal}
      AFTER:  RM{(totals.subtotalCents / 100).toFixed(2)}

      BEFORE: RM{totals.serviceCharge}
      AFTER:  RM{(totals.serviceChargeCents / 100).toFixed(2)}

      BEFORE: RM{totals.sst}
      AFTER:  RM{(totals.sstCents / 100).toFixed(2)}

      BEFORE: RM{totals.grandTotal}
      AFTER:  RM{(totals.grandTotalCents / 100).toFixed(2)}

    Context for each display site (use surrounding JSX as landmark to avoid wrong-line edits):
    - totals.subtotal is inside a div with span text "Subtotal"
    - totals.serviceCharge is inside a conditional block gated on bill.applyServiceCharge, with span text "Service Charge (10%)"
    - totals.sst is inside a conditional block gated on bill.applySST, with span text "SST (6%)"
    - totals.grandTotal is inside a div with span text "GRAND TOTAL" (uppercase, font-bold)

    After all three edits:
    - The file has no inline `function calculateTotals` definition
    - The file has `import { calculateTotals } from "@/lib/calculateTotals"` in the import block
    - The `const totals = calculateTotals(...)` callsite is unchanged
    - All four display sites use the new inline (v / 100).toFixed(2) pattern
    - No remaining references to `totals.subtotal`, `totals.serviceCharge`, `totals.sst`, or `totals.grandTotal`
  </action>
  <verify>
    <automated>cd /Users/irfanmurad/Developer/vessl/tongtong-bill-splitting && grep -n "import.*calculateTotals\|function calculateTotals\|totals\.subtotal\|totals\.serviceCharge\|totals\.sst\|totals\.grandTotal\|totals\.subtotalCents\|totals\.grandTotalCents" src/app/c/\[billId\]/page.tsx && pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep for "import.*calculateTotals.*@/lib/calculateTotals" in src/app/c/[billId]/page.tsx returns 1 match
    - grep for "function calculateTotals" in the file returns 0 matches (inline function deleted)
    - grep for "totals\.subtotal[^C]" in the file returns 0 matches (old pre-formatted field gone)
    - grep for "totals\.serviceCharge[^C]" in the file returns 0 matches (old pre-formatted field gone)
    - grep for "totals\.sst[^C]" in the file returns 0 matches (old pre-formatted field gone)
    - grep for "totals\.grandTotal[^C]" in the file returns 0 matches (old pre-formatted field gone)
    - grep for "totals\.subtotalCents" in the file returns 1 match (new display site)
    - grep for "totals\.serviceChargeCents" in the file returns 1 match (new display site)
    - grep for "totals\.sstCents" in the file returns 1 match (new display site)
    - grep for "totals\.grandTotalCents" in the file returns 1 match (new display site)
    - grep for "const totals = calculateTotals" in the file returns 1 match (callsite preserved)
    - pnpm tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Member view page imports calculateTotals from @/lib/calculateTotals, inline function removed, all four display sites updated to inline (v / 100).toFixed(2) formatting, TypeScript strict mode passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new boundaries | Pure refactor of an existing client component. No new external I/O. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-03 | Tampering | Display arithmetic in member view | accept | toFixed(2) is identical arithmetic to the old string fields (which were themselves (cents/100).toFixed(2)); TypeScript will catch field-name mismatches at compile time; no runtime behavior change |
| T-01.1-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan |
</threat_model>

<verification>
After task completes:
1. pnpm tsc --noEmit exits 0 (full project type check)
2. grep for "function calculateTotals" across src/app/c/[billId]/page.tsx returns 0
3. grep for "totals\.subtotalCents\|totals\.serviceChargeCents\|totals\.sstCents\|totals\.grandTotalCents" returns 4 matches (all display sites updated)
4. grep for "totals\.subtotal[^C]\|totals\.serviceCharge[^C]\|totals\.sst[^C]\|totals\.grandTotal[^C]" returns 0 matches (old field names gone)
5. src/components/RunningTotal.tsx and src/test/calculateTotals.test.ts unchanged (git diff --name-only confirms neither appears in diff)
</verification>

<success_criteria>
- c/[billId]/page.tsx imports from @/lib/calculateTotals with inline function removed
- All four display sites updated to cents arithmetic (v / 100).toFixed(2)
- totals callsite preserved as-is (const totals = calculateTotals(...))
- pnpm tsc --noEmit exits 0 across the full project
- RunningTotal.tsx and src/test/calculateTotals.test.ts untouched
</success_criteria>

<output>
Create `.planning/phases/01.1-tech-debt-dash-04-label-fix-calculatetotals-extraction/01.1-03-SUMMARY.md` when done.
</output>
