---
phase: 03-tongtong-aesthetic
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified:
  - src/app/create/page.tsx
  - src/app/dashboard/[billId]/page.tsx
  - src/components/BillSummaryCard.tsx
autonomous: true
requirements:
  - UI-01
  - UI-03
  - UI-04
  - UI-06
  - UI-07
  - UI-09
  - UI-10
  - UI-11
  - UI-12

must_haves:
  truths:
    - "Bill builder form content wrapped in .chit panel with .perforation dividers between sections"
    - "GENERATE LINK button has no border-radius (chop/stamp style, UI-07)"
    - "Dashboard loading states show skeleton chit cards (not LOADING... text)"
    - "Dashboard empty state: NOTHING HERE YET in .chit wrapper with COPY SHARE LINK bg-pen button (UI-11)"
    - "BillSummaryCard uses .chit class (not bg-paper-chit rounded)"
    - "Dashboard panels wrapped in .chit with .perforation dividers between sections"
    - ".rule-hairline and .dot-leader applied to BillSummaryCard item rows"
    - "Desktop 2-column layout intact (md:grid-cols remains unchanged)"
  artifacts:
    - path: "src/app/create/page.tsx"
      provides: "Bill builder with .chit form wrapper, .perforation dividers, GENERATE LINK button without rounded"
    - path: "src/app/dashboard/[billId]/page.tsx"
      provides: "Dashboard with .chit panels, skeleton loading, NOTHING HERE YET empty state, .perforation dividers"
    - path: "src/components/BillSummaryCard.tsx"
      provides: "BillSummaryCard using .chit class instead of bg-paper-chit rounded, with dot-leader item rows"
  key_links:
    - from: "src/app/dashboard/[billId]/page.tsx"
      to: "src/components/BillSummaryCard.tsx"
      via: "BillSummaryCard import — shared .chit class"
      pattern: "BillSummaryCard"
    - from: "src/app/create/page.tsx"
      to: "globals.css .chit + .perforation"
      via: "className chit on form content wrapper"
      pattern: "\"chit "
---

<objective>
Apply the chit aesthetic to the two organizer-facing screens (bill builder and dashboard) and upgrade BillSummaryCard to use the .chit class. These are Wave 3 organizer-only screens (D-07) — same visual language as Wave 2, applied to the creation and management flows.

Purpose: Complete the aesthetic across all four screens. After this plan, every surface in TongTong — landing, member view, bill builder, dashboard — uses paper colors, dot leaders, hairline rules, and perforation dividers.

Output:
- src/app/create/page.tsx — .chit form wrapper, .perforation between sections, no-rounded GENERATE LINK button
- src/app/dashboard/[billId]/page.tsx — skeleton loading, NOTHING HERE YET empty state, .chit panels, .perforation dividers
- src/components/BillSummaryCard.tsx — .chit class, dot-leader item rows
</objective>

<execution_context>
@/Users/irfanmurad/Developer/vessl/tongtong-bill-splitting/.claude/get-shit-done/workflows/execute-plan.md
@/Users/irfanmurad/Developer/vessl/tongtong-bill-splitting/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/irfanmurad/Developer/vessl/tongtong-bill-splitting/.planning/ROADMAP.md
@/Users/irfanmurad/Developer/vessl/tongtong-bill-splitting/.planning/phases/03-tongtong-aesthetic/03-CONTEXT.md
@/Users/irfanmurad/Developer/vessl/tongtong-bill-splitting/.planning/phases/03-tongtong-aesthetic/03-UI-SPEC.md

<interfaces>
<!-- create/page.tsx current structure (key lines): -->
<!-- Line 133: main className="min-h-screen bg-paper-table" -->
<!-- Line 134: div className="max-w-[480px] mx-auto px-4 py-8" -->
<!-- Line 186: items section div className="mt-6" — wrap in .chit -->
<!-- Line 265: GENERATE LINK button className="...rounded..." — remove rounded -->
<!-- ItemRow component: flex items-center gap-2 — form row, not a display row; no dot-leader needed -->

<!-- dashboard/[billId]/page.tsx current structure (key lines): -->
<!-- Lines 58–66: first loading guard (organizerSecret === null) — "LOADING..." text -->
<!-- Lines 85–93: second loading guard (billData === undefined) — "LOADING..." text -->
<!-- Lines 203–220: empty state "NO ONE'S JOINED YET" — replace with "NOTHING HERE YET" + .chit -->
<!-- Line 169: main className="min-h-screen bg-paper-table" (correct, keep) -->
<!-- Line 180: 2-column layout div className="md:grid md:grid-cols-[60%_40%] md:gap-8" (keep unchanged) -->
<!-- Line 184–246: left column content (progress bar, stats, people section) -->
<!-- Line 249–308: right column content (BillSummaryCard, quick actions) -->

<!-- BillSummaryCard.tsx current structure: -->
<!-- Line 23: container className="bg-paper-chit rounded p-4" — change to "chit p-4" -->
<!-- Lines 40–49: only a grand total row with flex justify-between — can add dot-leader -->
<!-- No individual item rows displayed — just count and total -->

<!-- globals.css key classes (all already defined): -->
<!-- .chit: bg + shadow -->
<!-- .dot-leader: flex + ::after dots -->
<!-- .rule-hairline: 0.5px border at 35% opacity -->
<!-- .perforation: dashed 1px line -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish bill builder form and upgrade BillSummaryCard</name>
  <files>src/app/create/page.tsx, src/components/BillSummaryCard.tsx</files>
  <read_first>
    - src/app/create/page.tsx (full file — 292 lines; read completely before editing)
    - src/components/BillSummaryCard.tsx (full file — 53 lines; read before editing)
  </read_first>
  <action>
    PART A — src/app/create/page.tsx (aesthetic polish, no logic changes):

    CHANGE 1 — Wrap the bill details form fields in a .chit container:
    The bill details section starts with the space-y-4 div containing the three input fields (title, venue, date). Wrap these fields in a div with className "chit p-4 mb-4". Keep the "space-y-4" inner structure. Add a perforation div (className "perforation my-4") after the chit form fields div.

    CHANGE 2 — Wrap the items section in a .chit container:
    The items section starts at div className="mt-6" (around line 186). Wrap the items content (h2 "Items", the item list or empty-state paragraph, and the ADD ITEM button) in a div with className "chit p-4 mb-4". Keep the h2 and item controls inside. Add a perforation div (className "perforation my-4") after this items chit section.

    CHANGE 3 — GENERATE LINK button: remove the "rounded" class from the button className (around line 265). The button should use "w-full h-12 bg-pen text-white uppercase font-bold text-base transition-opacity" without any rounded class — chop/stamp aesthetic per UI-07. Keep the conditional opacity/cursor disabled classes.

    CHANGE 4 — ADD ITEM button (secondary CTA): this button already correctly uses "border border-ink text-ink" style — not blue. Keep it as-is (not a primary CTA). However, remove the "rounded" class if present to match the chop aesthetic.

    PART B — src/components/BillSummaryCard.tsx:

    CHANGE 5 — Container class: Change line 23 className from "bg-paper-chit rounded p-4" to "chit p-4". The .chit class provides background and shadow; remove both "bg-paper-chit" and "rounded".

    CHANGE 6 — Grand total row: The flex justify-between div around line 41 already shows TOTAL and RM amount — add "dot-leader" to its className for dot alignment: className="dot-leader items-center".

    BillSummaryCard is a "use client" component (line 1) — keep that directive unchanged since it imports calculateTotals.
  </action>
  <verify>
    <automated>
      cd /Users/irfanmurad/Developer/vessl/tongtong-bill-splitting && pnpm exec tsc --noEmit 2>&1 | head -20
    </automated>
  </verify>
  <acceptance_criteria>
    create/page.tsx:
    - grep -c "\"chit " src/app/create/page.tsx returns at least 2 (two chit wrapper panels)
    - grep -c "perforation" src/app/create/page.tsx returns at least 2 (dividers between sections)
    - grep -c "rounded" src/app/create/page.tsx returns 0 (all rounded classes removed from GENERATE LINK and ADD ITEM buttons)

    BillSummaryCard.tsx:
    - grep -c "bg-paper-chit rounded" src/components/BillSummaryCard.tsx returns 0 (old class removed)
    - grep -c "\"chit " src/components/BillSummaryCard.tsx returns 1
    - grep -c "dot-leader" src/components/BillSummaryCard.tsx returns 1
    - pnpm exec tsc --noEmit exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Polish dashboard loading states, empty state, and panel wrappers</name>
  <files>src/app/dashboard/[billId]/page.tsx</files>
  <read_first>
    - src/app/dashboard/[billId]/page.tsx (full file — 314 lines; read completely before editing)
  </read_first>
  <action>
    Apply four targeted changes to src/app/dashboard/[billId]/page.tsx. No logic changes.

    CHANGE 1 — First loading guard (organizerSecret === null, around lines 58–66):
    Replace the "LOADING..." paragraph with a skeleton chit card. Main tag keeps className "min-h-screen bg-paper-table flex items-center justify-center". Inner content:
    - div className "chit max-w-[480px] w-full mx-4 p-4 animate-pulse" containing:
      - div className "h-4 bg-ink opacity-10 rounded mb-3 w-1/3"
      - div className "h-3 bg-ink opacity-10 rounded mb-2 w-full"
      - div className "h-3 bg-ink opacity-10 rounded mb-2 w-4/5"
      - div className "h-3 bg-ink opacity-10 rounded w-3/4"

    CHANGE 2 — Second loading guard (billData === undefined, around lines 85–93):
    Replace the "LOADING..." paragraph with the same skeleton chit card pattern (identical to CHANGE 1 above).

    CHANGE 3 — Empty state (payments?.length === 0 branch, around lines 203–220):
    Replace the existing dashed-border div with a .chit wrapper per UI-11. Copy the handleCopyShareLink button but update the wrapper and heading text:
    - div className "chit p-6 text-center" containing:
      - p className "text-xs font-bold uppercase text-ink tracking-widest mb-1 opacity-60" — text: "NOTHING HERE YET"
      - p className "text-sm text-ink opacity-60 mb-4" — text: "Share the link and they'll appear here." (use &apos; for apostrophe)
      - button type="button" onClick={handleCopyShareLink} className "bg-pen text-white text-xs h-10 px-5 uppercase tracking-widest cursor-pointer" — text: "COPY SHARE LINK"

    CHANGE 4 — Right column panel polish (dashboard right column, around lines 249–308):
    The right column contains BillSummaryCard (already upgraded in Task 1) and quick action buttons. Add a .perforation divider between the BillSummaryCard and the QUICK ACTIONS heading: insert div className "perforation my-4" between the BillSummaryCard closing tag and the h3 "QUICK ACTIONS" element.

    Additionally, add a .perforation div (className "perforation my-4") between the ProgressBar/StatsBar section and the h2 "PEOPLE" heading in the left column (between line ~197 and ~200 where the h2 appears).

    Keep the 2-column layout div (md:grid md:grid-cols-[60%_40%] md:gap-8) completely unchanged — UI-10 desktop responsiveness must not be broken.
  </action>
  <verify>
    <automated>
      cd /Users/irfanmurad/Developer/vessl/tongtong-bill-splitting && pnpm exec tsc --noEmit 2>&1 | head -20
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -c "animate-pulse" src/app/dashboard/\[billId\]/page.tsx returns 2 (two loading guards)
    - grep -c "LOADING\.\.\." src/app/dashboard/\[billId\]/page.tsx returns 0 (old text removed)
    - grep -c "NOTHING HERE YET" src/app/dashboard/\[billId\]/page.tsx returns 1 (UI-11 empty state)
    - grep -c "NO ONE" src/app/dashboard/\[billId\]/page.tsx returns 0 (old empty state heading removed)
    - grep -c "\"chit " src/app/dashboard/\[billId\]/page.tsx returns at least 2 (skeleton + empty state)
    - grep -c "perforation" src/app/dashboard/\[billId\]/page.tsx returns at least 2 (section dividers)
    - grep -c "md:grid-cols-\[60%_40%\]" src/app/dashboard/\[billId\]/page.tsx returns 1 (2-col layout intact)
    - pnpm exec tsc --noEmit exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| organizer localStorage → dashboard | organizerSecret must match; no logic changes in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Information Disclosure | Dashboard skeleton | accept | Skeleton shows placeholder bars only — no bill data displayed while loading |
| T-03-08 | Tampering | Dashboard layout change | accept | Only CSS classes changed; 2-column layout preserved; no logic or auth changes |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages — CSS class application only |
</threat_model>

<verification>
Run after both tasks complete:

1. pnpm exec tsc --noEmit — must exit 0
2. grep -c "rounded" src/app/create/page.tsx — must return 0
3. grep -c "perforation" src/app/create/page.tsx — must return at least 2
4. grep -c "animate-pulse" src/app/dashboard/[billId]/page.tsx — must return 2
5. grep -c "NOTHING HERE YET" src/app/dashboard/[billId]/page.tsx — must return 1
6. grep -c "LOADING\.\.\." src/app/dashboard/[billId]/page.tsx — must return 0
7. grep -c "bg-paper-chit rounded" src/components/BillSummaryCard.tsx — must return 0
8. grep -c "md:grid-cols-\[60%_40%\]" src/app/dashboard/[billId]/page.tsx — must return 1
</verification>

<success_criteria>
- Bill builder: .chit form panels, .perforation between sections, GENERATE LINK and ADD ITEM buttons have no rounded class
- Dashboard: both loading guards show skeleton chit (not "LOADING..." text), empty state shows "NOTHING HERE YET" in .chit with COPY SHARE LINK button, .perforation dividers between sections
- BillSummaryCard: uses .chit class (not bg-paper-chit rounded), dot-leader on total row
- Desktop 2-column layout on dashboard unchanged (md:grid-cols-[60%_40%] still present)
- TypeScript compiler exits clean
</success_criteria>

<output>
Create `.planning/phases/03-tongtong-aesthetic/03-04-SUMMARY.md` when done.
</output>
