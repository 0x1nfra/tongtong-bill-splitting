---
phase: 03-tongtong-aesthetic
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/app/page.tsx
  - src/components/DemoChit.tsx
  - src/components/SettleStamp.tsx
autonomous: true
requirements:
  - UI-05
  - UI-07
  - UI-08
  - LAND-01
  - LAND-02
  - LAND-03

must_haves:
  truths:
    - "Landing page shows 'tongtong.' logotype in Departure Mono with red period"
    - "Landing page shows tagline ABOVE demo chit and flavor copy BELOW demo chit (D-02 layout)"
    - "DemoChit renders kopitiam items with dot-leader alignment and SETTLED stamp at full opacity"
    - "START NEW BILL button is full-width, blue bg, no border-radius, min 48px height"
    - "SettleStamp animates with stamp-land thwack ONLY on pending→settled transition, not on mount"
    - "Ink-bleed filter applied to SETTLE stamp container (progressive enhancement)"
  artifacts:
    - path: "src/components/DemoChit.tsx"
      provides: "Server component with hardcoded kopitiam order and pre-landed SETTLED stamp"
      exports: ["DemoChit"]
    - path: "src/app/page.tsx"
      provides: "Full landing page: logotype, tagline, DemoChit, flavor copy, CTA"
    - path: "src/components/SettleStamp.tsx"
      provides: "Animation trigger via useRef prevStatus; ink-bleed filter on stamp containers"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/DemoChit.tsx"
      via: "import { DemoChit } from @/components/DemoChit"
      pattern: "import.*DemoChit.*from"
    - from: "src/components/SettleStamp.tsx stamp container"
      to: "globals.css animate-stamp-land / layout.tsx #ink-bleed filter"
      via: "className animate-stamp-land + style filter: url(#ink-bleed)"
      pattern: "animate-stamp-land|ink-bleed"
---

<objective>
Build the landing page (LAND-01/02/03) and upgrade SettleStamp with ink-bleed + thwack animation (UI-05). These are the highest-bounce-risk screens (D-06): the landing page converts new visitors and the stamp is the primary delight moment.

Purpose: Deliver the hero screen and key interaction that define TongTong's visual identity.

Output:
- src/components/DemoChit.tsx — new Server Component, hardcoded kopitiam data (D-01), pre-landed SETTLED stamp
- src/app/page.tsx — full landing page replacing the current stub; layout per D-02
- src/components/SettleStamp.tsx — useRef prev-prop animation guard; ink-bleed filter on both stamp states
</objective>

<execution_context>
@/Users/irfanmurad/Developer/vessl/tongtong-bill-splitting/.claude/get-shit-done/workflows/execute-plan.md
@/Users/irfanmurad/Developer/vessl/tongtong-bill-splitting/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/irfanmurad/Developer/vessl/tongtong-bill-splitting/.planning/ROADMAP.md
@/Users/irfanmurad/Developer/vessl/tongtong-bill-splitting/.planning/phases/03-tongtong-aesthetic/03-CONTEXT.md
@/Users/irfanmurad/Developer/vessl/tongtong-bill-splitting/.planning/phases/03-tongtong-aesthetic/03-UI-SPEC.md

<interfaces>
<!-- SettleStamp.tsx current implementation (src/components/SettleStamp.tsx): -->
<!-- "use client" -->
<!-- export type SettleStampStatus = "pending" | "settled" | "rejected" | null; -->
<!-- export function SettleStamp({ status }: Readonly<{ status: SettleStampStatus }>) -->
<!-- null|rejected → return null -->
<!-- pending → opacity-50 div with inline-block stamp + "AWAITING CONFIRMATION" p -->
<!-- settled → opacity-100 div with same stamp + "HAVE A GOOD ONE!" p in text-pen -->
<!-- Stamp container (both states): className="inline-block border-2 border-stamp rounded px-4 py-2" style={{ transform: "rotate(-6deg)" }} -->

<!-- globals.css animate-stamp-land (added by Plan 01): -->
<!-- --animate-stamp-land: stamp-land 300ms ease-out forwards; -->
<!-- @keyframes stamp-land: 0% scale(1.3) → 70% scale(0.95) → 100% scale(1.0), all at rotate(-6deg) -->

<!-- layout.tsx SVG filter (added by Plan 01): -->
<!-- <svg id="filters" aria-hidden="true"><defs><filter id="ink-bleed"><feTurbulence.../><feDisplacementMap scale="2.5" .../></filter></defs></svg> -->
<!-- Apply as: style={{ filter: "url(#ink-bleed)" }} on stamp container divs -->

<!-- page.tsx current stub (src/app/page.tsx): -->
<!-- Server Component, 28 lines, Link href="/create" already present -->
<!-- Full rewrite needed — replace entirely -->

<!-- BillSummaryCard analog for DemoChit: -->
<!-- Named export, no "use client", Readonly<{}> prop type, bg-paper-chit container div -->
<!-- Cents-to-display: (grandTotalCents / 100).toFixed(2) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DemoChit server component with kopitiam order and pre-landed stamp</name>
  <files>src/components/DemoChit.tsx</files>
  <read_first>
    - src/components/BillSummaryCard.tsx (analog: named export pattern, no "use client", Readonly props, bg-paper-chit container, cents-to-display format)
    - src/app/globals.css (verify .dot-leader, .chit, .perforation class names)
  </read_first>
  <action>
    Create src/components/DemoChit.tsx as a new Server Component (no "use client" directive — no hooks or Convex needed for hardcoded data).

    Export a named function DemoChit with no props (Readonly<Record<string, never>> or empty props).

    Hardcoded data (per D-01/specifics in CONTEXT.md):
    - DEMO_ITEMS constant typed as const array:
      { name: "Mee Goreng Mamak", price: 800, quantity: 1 }
      { name: "Teh Tarik", price: 300, quantity: 1 }
      { name: "Roti Canai", price: 250, quantity: 1 }
      { name: "Nasi Lemak Biasa", price: 500, quantity: 2 }
    - grandTotal computed via reduce: DEMO_ITEMS.reduce((acc, i) => acc + i.price * i.quantity, 0) = 2350 cents = RM23.50

    Container: div with className "chit p-4 max-w-[320px] mx-auto" and style={{ transform: "rotate(1.5deg)" }} (UI-09 fixed rotation for demo; no billId available)

    Header: p element, text "#TT-DEMO", className "text-xs font-bold uppercase tracking-widest text-ink opacity-60 mb-3"

    Item rows: map over DEMO_ITEMS, each row is a div with className "dot-leader py-1" containing:
    - Left span: className "text-sm text-ink" — item name, append " x{quantity}" if quantity > 1 in a span with className "opacity-60 ml-1 text-xs"
    - Right span: className "text-sm text-ink" — RM{((item.price * item.quantity) / 100).toFixed(2)}

    Perforation between items and total: div className "perforation my-3"

    Grand total row: div className "flex justify-between font-bold text-base text-ink", left span "GRAND TOTAL" (UPPERCASE, tracking-widest), right span RM{(grandTotal / 100).toFixed(2)}

    SETTLED stamp (pre-landed, no animation — server component): centered div, inside it a div with className "inline-block border-2 border-stamp rounded px-4 py-2" and style={{ transform: "rotate(-6deg)", filter: "url(#ink-bleed)" }}, inside that a span with className "text-3xl font-bold text-stamp uppercase tracking-widest" and style={{ fontFamily: "var(--font-stamp)" }} containing text "SETTLE"

    Do NOT import useState, useEffect, useRef, or any Convex hooks — this is a pure data display component.
  </action>
  <verify>
    <automated>
      cd /Users/irfanmurad/Developer/vessl/tongtong-bill-splitting && pnpm exec tsc --noEmit 2>&1 | head -20
    </automated>
  </verify>
  <acceptance_criteria>
    - src/components/DemoChit.tsx exists and is non-empty
    - grep -c "use client" src/components/DemoChit.tsx returns 0 (Server Component)
    - grep -c "export function DemoChit" src/components/DemoChit.tsx returns 1
    - grep -c "dot-leader" src/components/DemoChit.tsx returns at least 1
    - grep -c "Mee Goreng Mamak" src/components/DemoChit.tsx returns 1 (hardcoded data present)
    - grep -c "filter.*ink-bleed\|ink-bleed.*filter" src/components/DemoChit.tsx returns 1 (stamp filter present)
    - grep -c "SETTLE" src/components/DemoChit.tsx returns 1
    - grep -c "font-stamp" src/components/DemoChit.tsx returns 1 (Bungee stamp font applied)
    - pnpm exec tsc --noEmit exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rewrite landing page and upgrade SettleStamp with thwack animation</name>
  <files>src/app/page.tsx, src/components/SettleStamp.tsx</files>
  <read_first>
    - src/app/page.tsx (current 28-line stub — understand structure before replacing)
    - src/components/SettleStamp.tsx (full file — understand all render branches before adding animation)
  </read_first>
  <action>
    PART A — Rewrite src/app/page.tsx (full replacement):

    Server Component (no "use client"). Imports: import Link from "next/link" and import { DemoChit } from "@/components/DemoChit".

    Structure (D-02 locked layout order — tagline ABOVE chit, flavor copy BELOW chit):
    - Outer: main with className "min-h-screen flex flex-col items-center justify-center bg-paper-table px-4 py-12"
    - Logotype (UI-08): h1 with className "text-[28px] lowercase tracking-tight text-ink mb-1" and style={{ fontFamily: "var(--font-display)" }}. Text: "tongtong" followed by span with className "text-stamp" containing "."
    - Sub-logotype (LAND-01): p with className "text-xs font-bold uppercase tracking-widest text-ink mb-6 opacity-60". Text: "A CHIT FOR EVERYONE"
    - Tagline (LAND-02, above chit per D-02): p with className "text-xs font-bold uppercase tracking-widest text-ink mb-6 text-center max-w-[320px]". Text: "SPLIT THE BILL, NOT THE FRIENDSHIP."
    - Demo chit (LAND-01, D-01): DemoChit component
    - Flavor copy (LAND-02, below chit per D-02): p with className "mt-6 text-sm text-ink opacity-70 text-center max-w-[280px]" and style={{ fontFamily: "var(--font-handwriting)" }}. Text (use HTML entities): Eh ya, no more &ldquo;you transfer ah?&rdquo; drama lah. One chit. Everyone tandakan. Beres.
    - CTA (LAND-03): Link href="/create" with className "mt-6 block w-full max-w-[320px] bg-pen text-white uppercase font-bold text-xs tracking-widest min-h-[48px] flex items-center justify-center" (no rounded class — chop/stamp aesthetic per UI-07). Text: "START NEW BILL"

    PART B — Upgrade src/components/SettleStamp.tsx:

    Add useState, useEffect, useRef to the React import. Keep "use client" directive and the existing SettleStampStatus type export.

    Inside the SettleStamp function, before any conditional returns, add:
    - prevStatusRef: useRef<SettleStampStatus> initialized to status (NOT null — initializing to null would cause animation on mount for already-settled pages, violating D-05)
    - isAnimating: useState(false)
    - useEffect that fires on [status] changes:
      - captures prev = prevStatusRef.current
      - sets prevStatusRef.current = status
      - if status === "settled" AND prev !== "settled": setIsAnimating(true), setTimeout to setIsAnimating(false) after 350ms, returns cleanup that clears the timer
      - all other transitions: do nothing

    On the pending branch stamp container div: add style={{ transform: "rotate(-6deg)", filter: "url(#ink-bleed)" }} (ink-bleed as progressive enhancement; no animation on pending)

    On the settled branch stamp container div:
    - className: toggle animate-stamp-land conditionally — "inline-block border-2 border-stamp rounded px-4 py-2" plus " animate-stamp-land" when isAnimating is true
    - style: {{ transform: "rotate(-6deg)", filter: "url(#ink-bleed)" }}

    Keep all text content and opacity logic unchanged. Do not change the pending opacity-50 outer div or the "HAVE A GOOD ONE!" pen-colored text.
  </action>
  <verify>
    <automated>
      cd /Users/irfanmurad/Developer/vessl/tongtong-bill-splitting && pnpm exec tsc --noEmit 2>&1 | head -30
    </automated>
  </verify>
  <acceptance_criteria>
    page.tsx:
    - grep -c "use client" src/app/page.tsx returns 0 (Server Component)
    - grep -c "DemoChit" src/app/page.tsx returns 2 (import + usage)
    - grep -c "SPLIT THE BILL" src/app/page.tsx returns 1
    - grep -c "A CHIT FOR EVERYONE" src/app/page.tsx returns 1
    - grep -c "tongtong" src/app/page.tsx returns at least 1
    - grep -c "text-stamp" src/app/page.tsx returns at least 1 (red period on logotype)
    - grep -c "START NEW BILL" src/app/page.tsx returns 1
    - grep -c "font-display" src/app/page.tsx returns 1 (logotype font var)
    - grep -c "font-handwriting" src/app/page.tsx returns 1 (flavor copy font var)
    - grep -c "rounded" src/app/page.tsx returns 0 (chop/stamp CTA has no border-radius)

    SettleStamp.tsx:
    - grep -c "useRef" src/components/SettleStamp.tsx returns 1
    - grep -c "prevStatusRef" src/components/SettleStamp.tsx returns at least 3 (declaration + read + write)
    - grep -c "isAnimating" src/components/SettleStamp.tsx returns at least 3 (state decl + set + usage)
    - grep -c "animate-stamp-land" src/components/SettleStamp.tsx returns 1
    - grep -c "ink-bleed" src/components/SettleStamp.tsx returns 2 (one per stamp branch)
    - pnpm exec tsc --noEmit exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user browser → landing page | Unauthenticated visitors; landing page is fully static/server-rendered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | Landing page DemoChit | accept | Hardcoded data only — no real bill data; no user data exposed |
| T-03-04 | Denial of Service | SettleStamp animation timer | accept | Single 350ms setTimeout per transition; cleanup in useEffect return; no leak risk |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new npm packages — next/font built into Next.js; no install step |
</threat_model>

<verification>
Run after both tasks complete:

1. pnpm exec tsc --noEmit — must exit 0
2. grep -c "use client" src/app/page.tsx — must return 0 (Server Component)
3. grep -c "DemoChit" src/app/page.tsx — must return 2
4. grep -c "A CHIT FOR EVERYONE" src/app/page.tsx — must return 1
5. grep -c "SPLIT THE BILL" src/app/page.tsx — must return 1
6. grep -c "use client" src/components/DemoChit.tsx — must return 0
7. grep -c "animate-stamp-land" src/components/SettleStamp.tsx — must return 1
8. grep -c "ink-bleed" src/components/SettleStamp.tsx — must return 2
9. grep -c "prevStatusRef" src/components/SettleStamp.tsx — must return at least 3
</verification>

<success_criteria>
- Landing page: "tongtong." logotype (Departure Mono, red period), "A CHIT FOR EVERYONE", tagline above chit, flavor copy below chit, "START NEW BILL" blue chop button — all correct per D-02/LAND-01/02/03/UI-08
- DemoChit: kopitiam items with dot-leaders, SETTLED stamp pre-landed with ink-bleed filter, 1.5deg rotation
- SettleStamp: animation fires ONLY on pending→settled transition (not on mount), ink-bleed filter on both stamp states
</success_criteria>

<output>
Create `.planning/phases/03-tongtong-aesthetic/03-02-SUMMARY.md` when done.
</output>
