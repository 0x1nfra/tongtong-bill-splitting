---
phase: 03-tongtong-aesthetic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - public/fonts/DepartureMono-Regular.woff2
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-05

must_haves:
  truths:
    - "Departure Mono loads from /public/fonts/ — no cdnfonts.com request in DevTools Network tab"
    - "JetBrains Mono and Bungee load via next/font/google — no fonts.googleapis.com @import request"
    - "SETTLE stamp thwack animation class animate-stamp-land is available as a Tailwind utility"
    - "SVG ink-bleed filter (id=ink-bleed) is present in the DOM via layout.tsx body"
    - "All four font CSS variables resolve: --font-display, --font-body, --font-handwriting, --font-stamp"
  artifacts:
    - path: "public/fonts/DepartureMono-Regular.woff2"
      provides: "Self-hosted Departure Mono font file"
    - path: "src/app/globals.css"
      provides: "Animation token + @keyframes stamp-land inside @theme inline; @font-face block; no CDN @imports"
    - path: "src/app/layout.tsx"
      provides: "JetBrains_Mono + Bungee next/font/google imports; SVG filter inline in body"
  key_links:
    - from: "src/app/globals.css @font-face"
      to: "public/fonts/DepartureMono-Regular.woff2"
      via: "url(/fonts/DepartureMono-Regular.woff2)"
      pattern: "url\\(\"/fonts/DepartureMono-Regular\\.woff2\"\\)"
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css --font-body --font-stamp"
      via: "jetBrainsMono.variable bungee.variable in <html> className"
      pattern: "jetBrainsMono\\.variable.*bungee\\.variable"
    - from: "src/app/globals.css @theme inline"
      to: "animate-stamp-land Tailwind utility"
      via: "--animate-stamp-land: stamp-land 300ms ease-out forwards"
      pattern: "--animate-stamp-land"
---

<objective>
Deliver the font migration and CSS animation foundation that every downstream plan depends on. This plan is Wave 1 — nothing visual can be correct until fonts load from the right sources and the stamp animation class exists.

Purpose: Eliminate render-blocking CDN requests (2× Google Fonts @import + 1× cdnfonts.com @import) and establish the stamp-land animation utility class. Per D-09/D-10/D-11.

Output:
- `public/fonts/DepartureMono-Regular.woff2` — self-hosted font asset
- `src/app/globals.css` — CDN @imports removed, @font-face added, --animate-stamp-land + @keyframes stamp-land added inside @theme inline
- `src/app/layout.tsx` — JetBrains_Mono + Bungee next/font/google; SVG ink-bleed filter element in body
</objective>

<execution_context>
@/Users/irfanmurad/Developer/vessl/tongtong-bill-splitting/.claude/get-shit-done/workflows/execute-plan.md
@/Users/irfanmurad/Developer/vessl/tongtong-bill-splitting/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/irfanmurad/Developer/vessl/tongtong-bill-splitting/.planning/ROADMAP.md
@/Users/irfanmurad/Developer/vessl/tongtong-bill-splitting/.planning/phases/03-tongtong-aesthetic/03-CONTEXT.md
@/Users/irfanmurad/Developer/vessl/tongtong-bill-splitting/.planning/phases/03-tongtong-aesthetic/03-RESEARCH.md

<interfaces>
<!-- Current globals.css top (lines 1–3 — the CDN imports to remove): -->
<!-- @import url("https://fonts.googleapis.com/css2?family=JetBrains+Mono..."); -->
<!-- @import url("https://fonts.cdnfonts.com/css/departure-mono"); -->
<!-- @import "tailwindcss"; -->

<!-- Current @theme inline (lines 5–22): -->
<!-- --font-display: "Departure Mono", monospace; -->
<!-- --font-body: "JetBrains Mono", monospace; -->
<!-- --font-handwriting: "Shadows Into Light Two", cursive; -->
<!-- --font-stamp: "Bungee", sans-serif; -->
<!-- All four tokens already declared — font names are already correct. Only delivery changes. -->

<!-- Current layout.tsx (lines 2–11): -->
<!-- import { Shadows_Into_Light_Two } from "next/font/google"; -->
<!-- const shadowsIntoLightTwo = Shadows_Into_Light_Two({ weight: "400", subsets: ["latin"], display: "swap", variable: "--font-handwriting" }); -->
<!-- <html lang="en" className={`${shadowsIntoLightTwo.variable} h-full`}> -->
<!-- <body className="min-h-full flex flex-col"> -->

<!-- @layer base already declares: svg#filters { position: absolute; width: 0; height: 0; } -->
<!-- SVG must NOT use display:none — Firefox drops filter references from hidden SVGs -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download Departure Mono WOFF2 and update globals.css font delivery</name>
  <files>public/fonts/DepartureMono-Regular.woff2, src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (full file — lines 1–89, to see exact CDN @import lines to remove and @theme inline block to extend)
  </read_first>
  <action>
    Step 1 — Create the fonts directory and download the WOFF2:
    Run: mkdir -p /Users/irfanmurad/Developer/vessl/tongtong-bill-splitting/public/fonts
    Download DepartureMono-Regular.woff2 from the GitHub release. The release ZIP is at https://github.com/rektdeckard/departure-mono/releases/latest — use curl to download the ZIP, unzip it, find the WOFF2 (filename DepartureMono-Regular.woff2 per RESEARCH.md), and copy it to public/fonts/DepartureMono-Regular.woff2. Alternatively, if the direct WOFF2 URL is available, use curl with -L flag to follow redirects. After placing the file, verify it exists and has non-zero size.

    Step 2 — Edit src/app/globals.css with these three changes (atomic, same commit):
    REMOVE lines 1–2 (the two CDN @import lines):
      @import url("https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;700&family=Bungee&display=swap");
      @import url("https://fonts.cdnfonts.com/css/departure-mono");

    ADD before @import "tailwindcss" (line 1 of new file):
      @font-face {
        font-family: "Departure Mono";
        src: url("/fonts/DepartureMono-Regular.woff2") format("woff2");
        font-weight: 400;
        font-style: normal;
        font-display: swap;
      }

    APPEND inside the existing @theme inline { } block (after the --font-stamp line and before the closing brace):
      --animate-stamp-land: stamp-land 300ms ease-out forwards;

      @keyframes stamp-land {
        0%   { transform: rotate(-6deg) scale(1.3); }
        70%  { transform: rotate(-6deg) scale(0.95); }
        100% { transform: rotate(-6deg) scale(1.0); }
      }

    Critical ordering: @font-face block MUST appear BEFORE @import "tailwindcss" in the file. The @keyframes MUST be INSIDE the @theme inline { } block, not at root CSS level. Do not create a separate @theme block — append to the existing one. The @theme inline tokens --font-display, --font-body, --font-handwriting, --font-stamp are already declared with the correct family names; do not change them.
  </action>
  <verify>
    <automated>
      ls -la /Users/irfanmurad/Developer/vessl/tongtong-bill-splitting/public/fonts/DepartureMono-Regular.woff2 && grep -c "fonts.googleapis.com\|fonts.cdnfonts.com" /Users/irfanmurad/Developer/vessl/tongtong-bill-splitting/src/app/globals.css || true
    </automated>
  </verify>
  <acceptance_criteria>
    - public/fonts/DepartureMono-Regular.woff2 exists and has size > 0 bytes (ls -la shows non-zero)
    - grep -c "fonts.googleapis.com" src/app/globals.css returns 0 (CDN import removed)
    - grep -c "fonts.cdnfonts.com" src/app/globals.css returns 0 (CDN import removed)
    - grep -c "@font-face" src/app/globals.css returns 1
    - grep -c "DepartureMono-Regular.woff2" src/app/globals.css returns 1
    - grep -c "animate-stamp-land" src/app/globals.css returns 1 (inside @theme inline)
    - grep -c "stamp-land" src/app/globals.css returns at least 2 (--animate-stamp-land variable + @keyframes stamp-land name)
    - grep -c "@keyframes stamp-land" src/app/globals.css returns 1
    - The @font-face block appears BEFORE the line "@import "tailwindcss"" in the file
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migrate JetBrains Mono + Bungee to next/font/google and add SVG ink-bleed filter in layout.tsx</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx (full file — 35 lines, to see existing Shadows_Into_Light_Two pattern to replicate)
  </read_first>
  <action>
    Rewrite src/app/layout.tsx to add JetBrains_Mono and Bungee alongside the existing Shadows_Into_Light_Two font, and inject the SVG ink-bleed filter element.

    Import line: import { JetBrains_Mono, Bungee, Shadows_Into_Light_Two } from "next/font/google"

    Add two new font constant declarations using the exact same shape as the existing shadowsIntoLightTwo constant:
    - jetBrainsMono: JetBrains_Mono({ weight: "variable", subsets: ["latin"], display: "swap", variable: "--font-body" })
    - bungee: Bungee({ weight: "400", subsets: ["latin"], display: "swap", variable: "--font-stamp" })
    Keep the existing shadowsIntoLightTwo constant unchanged (weight: "400", subsets: ["latin"], display: "swap", variable: "--font-handwriting").

    Update the <html> element className template literal to include all three variables: ${jetBrainsMono.variable} ${bungee.variable} ${shadowsIntoLightTwo.variable} — keep h-full and lang="en".

    Add the SVG ink-bleed filter element as the first child inside <body>, before <ConvexClientProvider>:
      <svg id="filters" aria-hidden="true">
        <defs>
          <filter id="ink-bleed">
            <feTurbulence type="fractalNoise" baseFrequency="0.04" numOctaves="4" />
            <feDisplacementMap scale="2.5" xChannelSelector="R" yChannelSelector="G" />
          </filter>
        </defs>
      </svg>

    The SVG must NOT have display:none or visibility:hidden — it is already hidden by the svg#filters CSS rule in @layer base (position: absolute; width: 0; height: 0). Keep all existing metadata, imports, and the ConvexClientProvider wrapper unchanged.

    This file has no "use client" directive and must not get one — next/font/google functions are only valid in Server Components.
  </action>
  <verify>
    <automated>
      cd /Users/irfanmurad/Developer/vessl/tongtong-bill-splitting && pnpm exec tsc --noEmit --project tsconfig.json 2>&1 | head -30
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -c "JetBrains_Mono" src/app/layout.tsx returns 2 (import + const)
    - grep -c "Bungee" src/app/layout.tsx returns 2 (import + const)
    - grep -c "variable.*--font-body" src/app/layout.tsx returns 1
    - grep -c "variable.*--font-stamp" src/app/layout.tsx returns 1
    - grep -c "jetBrainsMono.variable" src/app/layout.tsx returns 1 (in html className)
    - grep -c "bungee.variable" src/app/layout.tsx returns 1 (in html className)
    - grep -c "ink-bleed" src/app/layout.tsx returns 2 (filter id + feTurbulence/feDisplacementMap block)
    - grep -c "use client" src/app/layout.tsx returns 0 (must remain a Server Component)
    - pnpm exec tsc --noEmit exits 0 (no TypeScript errors)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external CDN → public/fonts/ | Font asset downloaded from GitHub releases during plan execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | DepartureMono-Regular.woff2 download | mitigate | Download from official github.com/rektdeckard/departure-mono releases (SIL OFL verified in RESEARCH.md); verify file is non-zero size after download |
| T-03-02 | Information Disclosure | CDN removal | accept | Removing CDN requests eliminates Google/cdnfonts ping from user browsers; privacy improvement |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new npm packages installed in this plan — next/font is built into next@16.2.6 already |
</threat_model>

<verification>
Run after both tasks complete:

1. pnpm exec tsc --noEmit — must exit 0
2. grep -c "fonts.googleapis.com" src/app/globals.css — must return 0
3. grep -c "fonts.cdnfonts.com" src/app/globals.css — must return 0
4. grep -c "@font-face" src/app/globals.css — must return 1
5. grep -c "animate-stamp-land" src/app/globals.css — must return 1
6. ls public/fonts/DepartureMono-Regular.woff2 — must exist
7. grep -c "ink-bleed" src/app/layout.tsx — must return 2
8. grep -c "use client" src/app/layout.tsx — must return 0
</verification>

<success_criteria>
- CDN @imports removed: no network request to fonts.googleapis.com or fonts.cdnfonts.com on page load
- Departure Mono WOFF2 served from /fonts/ path (Next.js static serving from /public/)
- JetBrains Mono and Bungee CSS variables (--font-body, --font-stamp) injected onto <html> element via next/font/google
- animate-stamp-land Tailwind utility class available for use by SettleStamp component in Plan 02
- SVG filter id="ink-bleed" present in DOM body for filter: url(#ink-bleed) usage in Plan 02
- TypeScript compiler exits clean
</success_criteria>

<output>
Create `.planning/phases/03-tongtong-aesthetic/03-01-SUMMARY.md` when done.
</output>
