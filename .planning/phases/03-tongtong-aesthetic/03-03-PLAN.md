---
phase: 03-tongtong-aesthetic
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/app/c/[billId]/page.tsx
autonomous: true
requirements:
  - UI-01
  - UI-03
  - UI-04
  - UI-06
  - UI-09
  - UI-10
  - UI-12
  - UI-13

must_haves:
  truths:
    - "Member view items section uses .chit wrapper with .perforation dividers"
    - "Each item row uses .dot-leader class for name...price alignment"
    - ".rule-hairline <hr> appears between every item row"
    - "YOUR PORTION panel uses .chit class with pen border accents"
    - "YOUR PORTION rows (Subtotal, SC, SST, YOUR TOTAL) use .dot-leader alignment"
    - "Loading state renders a skeleton chit (animated gray bars) not a plain LOADING... text"
    - "Expired/null state renders EXPIRED stamp in a .chit wrapper with THIS CHIT HAS BEEN TORN UP heading"
    - "Chit outer container has deterministic 1–2° rotation derived from billId (UI-09)"
    - "CONFIRM PAYMENT button (I'VE PAID) has no border-radius — chop/stamp style"
  artifacts:
    - path: "src/app/c/[billId]/page.tsx"
      provides: "Fully aesthetically styled member/claim view — .chit, .dot-leader, .perforation, .rule-hairline, loading skeleton, expired state stamp"
  key_links:
    - from: "src/app/c/[billId]/page.tsx items section"
      to: "globals.css .dot-leader"
      via: "className dot-leader on each item row button"
      pattern: "dot-leader"
    - from: "src/app/c/[billId]/page.tsx outer container"
      to: "UI-09 one-imperfection rule"
      via: "const rotationDeg = (billId.charCodeAt(0) % 20) / 10 + 1; style={{ transform: rotate(${rotationDeg}deg) }}"
      pattern: "rotationDeg|charCodeAt"
---

<objective>
Apply the full chit aesthetic to the member/claim view — the page every WhatsApp link recipient lands on. This is the highest-traffic screen and the one most users will judge TongTong by.

Purpose: Transform the functional but unstyled member view into a chit-themed experience: paper panels, dot-leader item rows, hairline rules, perforation dividers, skeleton loading, EXPIRED stamp state.

Output: src/app/c/[billId]/page.tsx — polished with .chit, .dot-leader, .perforation, .rule-hairline, loading skeleton, expired stamp, and UI-09 outer rotation. No logic changes.
</objective>

<execution_context>
@/Users/irfanmurad/Developer/vessl/tongtong-bill-splitting/.claude/get-shit-done/workflows/execute-plan.md
@/Users/irfanmurad/Developer/vessl/tongtong-bill-splitting/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/irfanmurad/Developer/vessl/tongtong-bill-splitting/.planning/ROADMAP.md
@/Users/irfanmurad/Developer/vessl/tongtong-bill-splitting/.planning/phases/03-tongtong-aesthetic/03-CONTEXT.md
@/Users/irfanmurad/Developer/vessl/tongtong-bill-splitting/.planning/phases/03-tongtong-aesthetic/03-UI-SPEC.md

<interfaces>
<!-- Current c/[billId]/page.tsx structure (key lines): -->
<!-- Line 211–218: loading state → "LOADING..." text (replace with skeleton) -->
<!-- Line 221–234: null state → plain text (replace with .chit + EXPIRED stamp) -->
<!-- Line 272: main content wrapper → <div className="max-w-[480px] mx-auto px-4 py-6"> -->
<!-- Line 283: items section → <div className="bg-paper-chit p-4 mb-4"> (change to .chit) -->
<!-- Line 307–342: item button row → flex justify-between (add dot-leader class) -->
<!-- Line 413: YOUR PORTION panel → bg-paper-chit border-t-2 border-pen shadow-[...] border-l-4 border-l-pen (change to .chit + keep pen borders) -->
<!-- Lines 419–456: YOUR PORTION rows → flex justify-between (add dot-leader class) -->
<!-- Line 517–524: I'VE PAID button → bg-pen text-white (already correct; add rounded-none or remove rounded) -->
<!-- getRotation function already defined at lines 56–60 for claimant name rotation -->

<!-- globals.css classes: -->
<!-- .chit: background-color: #F4EFE6; box-shadow: 2px 4px 16px ... — do NOT add bg-paper-chit when using .chit -->
<!-- .dot-leader: display: flex; align-items: baseline — ::after pseudo fills with dots -->
<!-- .rule-hairline: border-color rgba(31,27,23,0.35); border-width 0.5px -->
<!-- .perforation: repeating-linear-gradient dashed line, height 1px -->

<!-- UI-09 rotation formula: const rotationDeg = (billId.charCodeAt(0) % 20) / 10 + 1; -->
<!-- Apply to the .chit wrapper div (not to main or the max-w outer div) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish loading state, expired state, and items section chit wrapper</name>
  <files>src/app/c/[billId]/page.tsx</files>
  <read_first>
    - src/app/c/[billId]/page.tsx (full file — 550 lines; read completely before making any edits to understand all current class names and structure)
  </read_first>
  <action>
    Make three targeted changes to src/app/c/[billId]/page.tsx. No logic changes — only class names, wrapper elements, and UI state presentation.

    CHANGE 1 — Loading state (bill === undefined branch, around lines 211–219):
    Replace the "LOADING..." paragraph with a skeleton chit card. New content inside main:
    - div className "chit max-w-[480px] w-full mx-4 p-4 animate-pulse" containing:
      - div className "h-4 bg-ink opacity-10 rounded mb-3 w-1/3" (no children)
      - div className "h-3 bg-ink opacity-10 rounded mb-2 w-full"
      - div className "h-3 bg-ink opacity-10 rounded mb-2 w-4/5"
      - div className "h-3 bg-ink opacity-10 rounded mb-2 w-full"
      - div className "h-3 bg-ink opacity-10 rounded w-3/4"
    Keep the outer main tag with existing className "min-h-screen bg-paper-table flex items-center justify-center".

    CHANGE 2 — Expired/null state (bill === null branch, around lines 221–234):
    Replace the plain div with a .chit wrapper containing an EXPIRED stamp followed by the existing heading and body text. New content inside main (keep existing main tag with px-4 added):
    - div className "chit max-w-[360px] w-full p-6 text-center mx-auto" containing:
      - div className "mb-4 flex justify-center" containing:
        - div className "inline-block border-2 border-stamp px-4 py-2" style={{ transform: "rotate(-6deg)" }} containing:
          - span className "text-2xl font-bold text-stamp uppercase tracking-widest" style={{ fontFamily: "var(--font-stamp)" }} — text: "EXPIRED"
      - h1 className "text-xl font-bold uppercase text-ink tracking-widest mb-3" — text: "THIS CHIT HAS BEEN TORN UP"
      - p className "text-sm text-ink opacity-60" — text: "The link may have expired or the chit was closed."

    CHANGE 3 — Items section container (around line 283):
    Change: className="bg-paper-chit p-4 mb-4"
    To: className="chit p-4 mb-4"
    (The .chit class already provides bg-paper-chit background color — do not duplicate bg-paper-chit)

    Add a perforation divider div (className "perforation mb-3") after the "ITEMS" header paragraph and before the items.map(). Also add a perforation divider (className "perforation mt-3") after the items.map() closes and before the closing tag of the items section div.
  </action>
  <verify>
    <automated>
      cd /Users/irfanmurad/Developer/vessl/tongtong-bill-splitting && pnpm exec tsc --noEmit 2>&1 | head -20
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -c "animate-pulse" src/app/c/\[billId\]/page.tsx returns 1 (skeleton loading)
    - grep -c "LOADING\.\.\." src/app/c/\[billId\]/page.tsx returns 0 (old text removed)
    - grep -c "EXPIRED" src/app/c/\[billId\]/page.tsx returns 1 (expired stamp text)
    - grep -c "font-stamp" src/app/c/\[billId\]/page.tsx returns 1 (Bungee on EXPIRED stamp)
    - grep -c "THIS CHIT HAS BEEN TORN UP" src/app/c/\[billId\]/page.tsx returns 1 (heading preserved)
    - grep -c "\"chit " src/app/c/\[billId\]/page.tsx returns at least 2 (items section + skeleton + expired)
    - grep -c "bg-paper-chit p-4 mb-4" src/app/c/\[billId\]/page.tsx returns 0 (old class replaced)
    - grep -c "perforation" src/app/c/\[billId\]/page.tsx returns at least 2 (dividers added)
    - pnpm exec tsc --noEmit exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Apply dot-leaders, hairline rules, YOUR PORTION polish, rotation, and button fix</name>
  <files>src/app/c/[billId]/page.tsx</files>
  <read_first>
    - src/app/c/[billId]/page.tsx (already modified by Task 1 — read current state before Task 2 edits)
  </read_first>
  <action>
    Continue editing src/app/c/[billId]/page.tsx with these four targeted changes. No logic changes.

    CHANGE 4 — Item row dot-leaders and hairline rules:
    Each item is wrapped in a div with className "border-b border-ink border-opacity-10 last:border-0" (around line 308). Replace this wrapping approach with hairline rules between items:
    - Change the outer item div className from "border-b border-ink border-opacity-10 last:border-0" to just remove the border classes (no border on item div itself).
    - Add a hr element with className "rule-hairline" between items — use this pattern: render an hr after each item div except the last one. The simplest approach: add className "rule-hairline" to an hr rendered after the tappable button row section (after claimants + inline entry) for all items except the last. Use the index from .map((item, index) => ...) — render hr only when index < items.length - 1.
    - On the tappable button element (the button tag, around line 314): add "dot-leader" to its className so the item name and price are dot-aligned. The button already uses flex with justify-between — add dot-leader to leverage the ::after pseudo-element. The class list should begin with "dot-leader w-full min-h-[48px]..."

    CHANGE 5 — YOUR PORTION panel:
    Around line 413: Replace className "bg-paper-chit border-t-2 border-pen p-4 shadow-[0_2px_12px_rgba(31,27,23,0.08)] border-l-4 border-l-pen mb-4" with "chit border-t-2 border-pen border-l-4 border-l-pen p-4 mb-4". The .chit class provides background and shadow — remove the manual shadow and bg-paper-chit.

    YOUR PORTION rows: Each row div uses "flex justify-between text-sm text-ink" — add "dot-leader" to these className strings to enable dot-leader alignment on Subtotal, SC, SST rows. Also add "dot-leader" to the YOUR TOTAL row (the one with "flex justify-between font-bold text-base text-ink border-t border-ink mt-2 pt-2").

    CHANGE 6 — Chit outer rotation (UI-09):
    Before the return statement of the main render (after all early returns), compute:
    const rotationDeg = (billId.charCodeAt(0) % 20) / 10 + 1;
    Apply this rotation to the items section .chit div and the YOUR PORTION .chit div by adding style={{ transform: `rotate(${rotationDeg}deg)` }} to those chit containers. One-imperfection rule: apply rotation to ONE primary chit panel (the items panel). Do not apply to every div or to the main element.

    CHANGE 7 — I'VE PAID button border-radius fix (UI-07):
    Around line 521: the button has className containing "rounded" implicitly from utility scan. Check and remove any "rounded" or "rounded-*" class from the bg-pen button. The className should be "w-full h-12 bg-pen text-white uppercase font-bold text-sm tracking-widest flex items-center justify-center disabled:opacity-50 disabled:cursor-not-allowed" — no border-radius classes.
  </action>
  <verify>
    <automated>
      cd /Users/irfanmurad/Developer/vessl/tongtong-bill-splitting && pnpm exec tsc --noEmit 2>&1 | head -20
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -c "rule-hairline" src/app/c/\[billId\]/page.tsx returns at least 1 (hairlines between rows)
    - grep -c "dot-leader" src/app/c/\[billId\]/page.tsx returns at least 3 (item button row + YOUR PORTION rows)
    - grep -c "rotationDeg" src/app/c/\[billId\]/page.tsx returns at least 2 (const declaration + usage)
    - grep -c "charCodeAt" src/app/c/\[billId\]/page.tsx returns at least 2 (getRotation func already present + rotationDeg)
    - grep -c "chit border-t-2 border-pen border-l-4" src/app/c/\[billId\]/page.tsx returns 1 (YOUR PORTION .chit class)
    - grep -c "shadow-\[0_2px" src/app/c/\[billId\]/page.tsx returns 0 (manual shadow replaced by .chit)
    - pnpm exec tsc --noEmit exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external user → member view | Unauthenticated visitors arriving via WhatsApp link |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | Member view skeleton | accept | Skeleton shows placeholder bars only — no bill data leaked during loading |
| T-03-06 | Elevation of Privilege | Member view auth | accept | No auth changes in this plan — claimantSession logic unchanged |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages — CSS class application only |
</threat_model>

<verification>
Run after both tasks complete:

1. pnpm exec tsc --noEmit — must exit 0
2. grep -c "animate-pulse" src/app/c/[billId]/page.tsx — must return 1
3. grep -c "LOADING\.\.\." src/app/c/[billId]/page.tsx — must return 0
4. grep -c "EXPIRED" src/app/c/[billId]/page.tsx — must return 1
5. grep -c "rule-hairline" src/app/c/[billId]/page.tsx — must return at least 1
6. grep -c "dot-leader" src/app/c/[billId]/page.tsx — must return at least 3
7. grep -c "rotationDeg" src/app/c/[billId]/page.tsx — must return at least 2
8. grep -c "perforation" src/app/c/[billId]/page.tsx — must return at least 2
</verification>

<success_criteria>
- Member view items section: .chit container, .perforation dividers, .dot-leader on item button rows, .rule-hairline between items
- YOUR PORTION panel: .chit class, pen-colored borders, dot-leader on all rows
- Loading state: skeleton chit card with animated gray bars (no "LOADING..." text)
- Expired state: EXPIRED stamp (Bungee, red, rotated) inside .chit wrapper with correct copy
- Outer rotation: deterministic 1–2° from billId charCode on primary items chit panel (UI-09)
- I'VE PAID button: no border-radius (chop/stamp style)
</success_criteria>

<output>
Create `.planning/phases/03-tongtong-aesthetic/03-03-SUMMARY.md` when done.
</output>
