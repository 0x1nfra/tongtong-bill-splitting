---
phase: 04-bonus-features
plan: 04
type: execute
wave: 3
depends_on: [04-03]
files_modified:
  - src/components/ThemeProvider.tsx
  - src/components/ThemeToggle.tsx
  - src/components/SignInButton.tsx
  - src/app/layout.tsx
  - src/app/globals.css
autonomous: false
requirements: [BONUS-06]

user_setup:
  - service: next-themes
    why: "Dark mode FOUC prevention and localStorage persistence"
    env_vars: []
    dashboard_config:
      - task: "Verify package legitimacy before install"
        location: "npmjs.com/package/next-themes — confirm version 0.4.6, downloads ~1M/wk, zero deps, MIT, github.com/pacocoursey/next-themes"

must_haves:
  truths:
    - "All pages show a fixed top-right theme toggle button cycling LIGHT / DARK / AUTO on click"
    - "In dark mode, the page background switches to #0D1B2A, chit surfaces to #1A2942, body text to #A8C0E0, pen blue to #5B8DD9 — stamp red #B91C1C stays unchanged"
    - "Theme preference persists across page reloads via localStorage (managed by next-themes)"
    - "No white flash on initial page load in dark mode (FOUC prevented by next-themes + suppressHydrationWarning on html tag)"
    - "SignIn.test.tsx tests pass GREEN after SignInButton.tsx is created"
    - "All existing tests remain green"
  artifacts:
    - path: "src/components/ThemeProvider.tsx"
      provides: "Client wrapper for NextThemesProvider with attribute='data-theme'"
    - path: "src/components/ThemeToggle.tsx"
      provides: "Theme toggle button — fixed top-right, cycles LIGHT/DARK/AUTO, uses useTheme()"
    - path: "src/components/SignInButton.tsx"
      provides: "SignInButton stub rendering SIGN IN WITH GOOGLE button (turns SignIn.test.tsx GREEN)"
    - path: "src/app/layout.tsx"
      provides: "ThemeProvider wrapper + suppressHydrationWarning on html tag + ThemeToggle in body"
    - path: "src/app/globals.css"
      provides: "@custom-variant dark + @layer base [data-theme=dark] token overrides"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/ThemeProvider.tsx"
      via: "ThemeProvider wraps ConvexClientProvider in body"
      pattern: "ThemeProvider.*ConvexClientProvider"
    - from: "src/app/globals.css"
      to: "Tailwind dark: utilities"
      via: "@custom-variant dark (&:where([data-theme=dark], [data-theme=dark] *))"
      pattern: "@custom-variant dark"
    - from: "src/components/ThemeToggle.tsx"
      to: "next-themes useTheme()"
      via: "const { theme, setTheme } = useTheme()"
      pattern: "useTheme"
---

<objective>
Implement the BONUS-06 dark mode "carbon copy" theme: install next-themes (after human verification), add @custom-variant dark and token overrides to globals.css, create ThemeProvider and ThemeToggle components, wire ThemeProvider into layout.tsx, and create SignInButton stub to turn the SignIn.test.tsx RED test GREEN.

Purpose: Users can toggle light/dark/system theme from any screen. The carbon-copy palette (navy background, light blue text) visually distinguishes dark mode while preserving the stamp-red brand identity.
Output: ThemeProvider.tsx, ThemeToggle.tsx, SignInButton.tsx (new), layout.tsx (updated), globals.css (updated).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-bonus-features/04-CONTEXT.md
@.planning/phases/04-bonus-features/04-UI-SPEC.md
@.planning/phases/04-bonus-features/04-PATTERNS.md

@.planning/phases/04-bonus-features/04-01-SUMMARY.md
@.planning/phases/04-bonus-features/04-03-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts the executor needs — extracted from codebase. -->

From src/app/layout.tsx (current provider nesting — lines 52-53):
```typescript
// Current:
<ConvexClientProvider>{children}</ConvexClientProvider>

// After this plan:
<ThemeProvider>
  <ConvexClientProvider>
    {children}
    <ThemeToggle />  {/* or ThemeToggle in body before ConvexClientProvider — fixed position */}
  </ConvexClientProvider>
</ThemeProvider>

// html tag (line 42): add suppressHydrationWarning
<html lang="en" suppressHydrationWarning className={...}>
```

From src/app/globals.css lines 1-9 (insertion point for dark variant):
```css
/* Line 9: @import "tailwindcss" */
/* Insert AFTER line 9 — @custom-variant must come after @import */
@custom-variant dark (&:where([data-theme=dark], [data-theme=dark] *));
```

From src/app/globals.css @theme block (lines 11-37) — DO NOT modify this block; only add @layer base override:
```css
@theme inline {
  --color-paper-chit: #F4EFE6;   /* light value — unchanged */
  --color-paper-table: #EEEAE2;  /* light value — unchanged */
  --color-ink: #1F1B17;          /* light value — unchanged */
  --color-pen: #1E40AF;          /* light value — unchanged */
  --color-stamp: #B91C1C;        /* UNCHANGED in both modes */
}
/* Add AFTER the @theme block: */
@layer base {
  [data-theme="dark"] {
    --color-paper-table: #0D1B2A;
    --color-paper-chit: #1A2942;
    --color-ink: #A8C0E0;
    --color-pen: #5B8DD9;
    /* --color-stamp intentionally omitted — must remain #B91C1C */
  }
  [data-theme="dark"] .chit {
    box-shadow: 2px 4px 16px rgba(0, 0, 0, 0.4), 0 1px 3px rgba(0, 0, 0, 0.25);
  }
  [data-theme="dark"] body::before {
    opacity: 0.04;
  }
  /* Dark mode body background — override hardcoded hex in body rule (globals.css line 40) */
  [data-theme="dark"] body {
    background-color: #0D1B2A;
    color: #A8C0E0;
  }
}
```

From src/components/ConvexClientProvider.tsx (ThemeProvider follows same shape):
```typescript
"use client";
export function ConvexClientProvider({ children }: { children: React.ReactNode }) {
  return <ConvexProvider client={convex}>{children}</ConvexProvider>;
}
// ThemeProvider uses same pattern: "use client", named export, NextThemesProvider wrapper
```

From 04-UI-SPEC.md (ThemeToggle button spec):
```
Placement: fixed top-right corner, 16px from edge (right-4 top-4), z-50
Size: min-h-11 min-w-11 (44px WCAG touch target)
Labels: LIGHT / DARK / AUTO (cycle: light → dark → system → light)
Style: border border-ink text-ink text-xs uppercase tracking-widest
Active state (current mode): border-pen text-pen
Font: font-[family-name:var(--font-body)] (JetBrains Mono)
Transition: transition-colors duration-200
```

From 04-UI-SPEC.md (SignInButton stub spec — BONUS-05 deferred, stub only):
```
Button text: SIGN IN WITH GOOGLE
Style: bg-pen text-white h-12 w-full uppercase tracking-widest text-xs
Note: no onClick behavior needed — this is a stub to turn SignIn.test.tsx GREEN
```
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Package legitimacy verification before installing next-themes. The RESEARCH.md Package Legitimacy Audit flagged next-themes as [ASSUMED] because slopcheck was unavailable at research time. Manual verification required before any install.</what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/next-themes
    2. Confirm: publisher is pacocoursey, version 0.4.6 available, download count ~1M/week range, zero dependencies listed, MIT license
    3. Visit https://github.com/pacocoursey/next-themes to confirm public source, maintained repo, not a fork of an unknown package
    4. If any of the above does not match expectations (different publisher, suspicious recent upload, unexpected dependencies), type "REJECT" to stop installation
    5. If verified legitimate, type "APPROVED" to proceed
  </how-to-verify>
  <resume-signal>Type "APPROVED" to install next-themes and continue, or "REJECT" to abort this plan</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install next-themes + create ThemeProvider and SignInButton components</name>
  <files>src/components/ThemeProvider.tsx, src/components/SignInButton.tsx</files>
  <read_first>
    - src/components/ConvexClientProvider.tsx — read in full; ThemeProvider follows the exact same "use client" wrapper pattern
    - .planning/phases/04-bonus-features/04-PATTERNS.md — read "src/components/ThemeProvider.tsx" pattern section
    - .planning/phases/04-bonus-features/04-RESEARCH.md — read "Standard Stack" section for next-themes version (0.4.6) and "Pattern 2: Tailwind v4 Dark Mode" and "next-themes ThemeProvider in layout.tsx" code example
    - src/test/SignIn.test.tsx — read the test to understand the exact component export name and button role/name expected
  </read_first>
  <action>
    Three changes:

    1. Install next-themes: run `pnpm add next-themes@0.4.6` from the project root. Use the exact version from RESEARCH.md Standard Stack.

    2. Create src/components/ThemeProvider.tsx. Add `"use client"` directive. Import `ThemeProvider as NextThemesProvider` from `"next-themes"`. Export a named function `ThemeProvider` accepting `{ children: React.ReactNode }`. Return `<NextThemesProvider attribute="data-theme" defaultTheme="system" enableSystem>{children}</NextThemesProvider>`. Use `Readonly<{ children: React.ReactNode }>` for props type (project convention). Named export only (no default export).

    3. Create src/components/SignInButton.tsx. Add `"use client"` directive. Export a named function `SignInButton` accepting no props. Returns a `<button type="button" className="bg-pen text-white h-12 w-full uppercase tracking-widest text-xs font-[family-name:var(--font-body)]">SIGN IN WITH GOOGLE</button>`. This is a STUB — no onClick behavior. No imports from next-themes or Convex auth (BONUS-05 deferred per D-08). Add a comment: `// BONUS-05 deferred per D-08 — stub only; no OAuth wiring`.
  </action>
  <verify>
    <automated>cd /Users/irfanmurad/Developer/vessl/tongtong-bill-splitting && pnpm test -- SignIn 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - next-themes appears in package.json dependencies at version 0.4.6
    - pnpm-lock.yaml updated (run `pnpm install` produces no lockfile changes)
    - src/components/ThemeProvider.tsx exists with `"use client"` directive
    - ThemeProvider uses `attribute="data-theme"` and `defaultTheme="system"` and `enableSystem`
    - src/components/SignInButton.tsx exists with `"use client"` directive
    - SignInButton renders a button with text "SIGN IN WITH GOOGLE" (case-insensitive match in test)
    - Comment in SignInButton.tsx references D-08 deferral
    - `pnpm test -- SignIn` exits 0 (previously RED test now GREEN)
  </acceptance_criteria>
  <done>next-themes installed; ThemeProvider and SignInButton stubs created; SignIn smoke test passes</done>
</task>

<task type="auto">
  <name>Task 2: Create ThemeToggle + wire ThemeProvider into layout + add dark tokens to globals.css</name>
  <files>src/components/ThemeToggle.tsx, src/app/layout.tsx, src/app/globals.css</files>
  <read_first>
    - src/app/layout.tsx — read in full; the ThemeProvider wraps ConvexClientProvider; html tag needs suppressHydrationWarning; ThemeToggle is placed inside body (after the SVG filters, before ConvexClientProvider — or as a sibling inside it)
    - src/app/globals.css — read in full; @custom-variant dark must be inserted after `@import "tailwindcss"` (line 9) and before `@theme inline`; @layer base dark overrides added after @theme block
    - .planning/phases/04-bonus-features/04-PATTERNS.md — read "src/app/layout.tsx" and "src/app/globals.css" sections
    - .planning/phases/04-bonus-features/04-UI-SPEC.md — read "Theme Toggle Button Visual Contract" and "Dark Mode Implementation Contract" sections
    - node_modules/next/dist/docs/ — check for any layout.tsx provider nesting warnings in Next.js 16 before modifying layout.tsx
  </read_first>
  <action>
    Three changes:

    1. Create src/components/ThemeToggle.tsx. Mark `"use client"`. Import `useTheme` from `"next-themes"`. Import `{ useEffect, useState }` from `"react"`. The component must handle hydration: use a `mounted` state initialized to false, set to true in useEffect — render null until mounted (prevents SSR hydration mismatch on the toggle label). After mounted: const `{ theme, setTheme } = useTheme()`. Cycle order on click: `theme === "light" ? setTheme("dark") : theme === "dark" ? setTheme("system") : setTheme("light")`. Button label: `theme === "dark" ? "DARK" : theme === "system" ? "AUTO" : "LIGHT"`. Active state class: when the current label matches the active theme, use `border-pen text-pen`; otherwise use `border-ink text-ink`. Full className: `fixed top-4 right-4 z-50 min-h-11 min-w-11 px-3 border text-xs uppercase tracking-widest transition-colors duration-200 font-[family-name:var(--font-body)] cursor-pointer`. Export as named `ThemeToggle`.

    2. Modify src/app/layout.tsx:
       - Add import: `import { ThemeProvider } from "@/components/ThemeProvider"` alongside the existing ConvexClientProvider import
       - Add import: `import { ThemeToggle } from "@/components/ThemeToggle"` 
       - Add `suppressHydrationWarning` to the `<html>` tag (line 42): `<html lang="en" suppressHydrationWarning className={...}>`
       - Wrap body contents: change `<ConvexClientProvider>{children}</ConvexClientProvider>` to `<ThemeProvider><ConvexClientProvider>{children}</ConvexClientProvider></ThemeProvider>`
       - Add `<ThemeToggle />` as a direct child inside `<body>` AFTER the SVG filters block and BEFORE `<ThemeProvider>` (ThemeToggle is fixed-position so placement in DOM tree is flexible, but placing it before the provider wrapper avoids any z-index conflict with ConvexClientProvider's render tree). Alternatively, place it inside `<ConvexClientProvider>` as a sibling to `{children}` — pick the approach that compiles without TypeScript error on ReactNode children.

    3. Modify src/app/globals.css:
       - After line 9 (`@import "tailwindcss"`), before the `@theme inline` block — insert the `@custom-variant dark` declaration: `@custom-variant dark (&:where([data-theme=dark], [data-theme=dark] *));`
       - After the closing `}` of the `@theme inline` block (after the `@keyframes stamp-land` block closes), insert the `@layer base` override block with: `[data-theme="dark"]` rule containing the four token overrides (paper-table, paper-chit, ink, pen — omit stamp), the `.chit` shadow override, the `body::before` opacity override, and a `body` background/color override matching the token values (because the existing `body { background-color: #EEEAE2; color: #1F1B17; }` rule uses hardcoded hex — it must be overridden for dark mode).
       - Do NOT modify the existing `@theme inline` block. Do NOT change any existing CSS rules. Only add the two new blocks.
  </action>
  <verify>
    <automated>cd /Users/irfanmurad/Developer/vessl/tongtong-bill-splitting && pnpm test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ThemeToggle.tsx exists with `"use client"`, `useTheme` import, mounted guard, and fixed top-right positioning classes
    - ThemeToggle cycles light → dark → system on click via setTheme
    - ThemeToggle renders null until mounted (no SSR hydration mismatch)
    - src/app/layout.tsx html tag has `suppressHydrationWarning` attribute
    - src/app/layout.tsx body wraps ConvexClientProvider with ThemeProvider
    - src/app/globals.css contains `@custom-variant dark (&:where([data-theme=dark], [data-theme=dark] *))` after `@import "tailwindcss"` and before `@theme`
    - src/app/globals.css contains `@layer base { [data-theme="dark"] { --color-paper-table: #0D1B2A` block
    - globals.css dark layer does NOT override `--color-stamp` (red must be preserved)
    - `pnpm test` exits 0 — all tests green including SignIn
  </acceptance_criteria>
  <done>Dark mode fully wired: ThemeToggle renders on all pages; carbon-copy palette activates under data-theme=dark; FOUC prevented; full test suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage (theme key) → ThemeProvider | next-themes reads/writes "theme" key; aesthetic preference only — no auth data |
| npm registry → pnpm add | next-themes package must be verified before install (package audit gate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | pnpm add next-themes | mitigate | Blocking human checkpoint before install — executor must verify npmjs.com/package/next-themes shows pacocoursey as publisher, ~1M/wk downloads, zero deps, MIT license before proceeding |
| T-04-W3-01 | Information Disclosure | localStorage theme key | accept | Theme preference is aesthetic only; no auth data, no session tokens, no user identity stored in theme key (RESEARCH.md Dark mode toggle leaking auth state: N/A) |
| T-04-W3-02 | Spoofing | ThemeToggle — no auth required to toggle | accept | Theme is a client-side preference with no security boundary; toggling theme has no server-side effect |
| T-04-05 | Spoofing | SignInButton stub | accept | Per D-08: BONUS-05 is deferred; SignInButton is a non-functional stub with no onClick handler; no OAuth wiring; no auth state change possible |
</threat_model>

<verification>
`pnpm test -- SignIn` exits 0 — SignInButton tests GREEN
`pnpm test` — full suite green (all 75+ existing tests + 3 new passing tests)
grep -n "@custom-variant dark" src/app/globals.css — appears after @import line
grep -n "suppressHydrationWarning" src/app/layout.tsx — appears on html tag
grep -n "ThemeProvider" src/app/layout.tsx — appears in import and JSX
grep -n "color-stamp" src/app/globals.css — dark layer block must NOT contain --color-stamp override
Manual verification (cannot automate): toggle to dark mode in browser; confirm bg is dark navy, text is light blue; stamp remains red; preference persists on reload
</verification>

<success_criteria>
- next-themes@0.4.6 installed after human verification checkpoint
- ThemeProvider.tsx: client wrapper with attribute="data-theme", defaultTheme="system", enableSystem
- ThemeToggle.tsx: fixed top-right, 44px touch target, cycles LIGHT/DARK/AUTO, mounted guard prevents SSR flash
- SignInButton.tsx: stub renders "SIGN IN WITH GOOGLE" button; no OAuth wiring; D-08 comment present
- layout.tsx: suppressHydrationWarning on html, ThemeProvider wraps ConvexClientProvider, ThemeToggle rendered
- globals.css: @custom-variant dark added after @import; @layer base [data-theme="dark"] block with 4 token overrides; --color-stamp intentionally absent from dark overrides
- Full test suite green
- T-04-05 (hand-rolled JWT) not triggered — no JWT signing; SignInButton is a stub with no auth logic
</success_criteria>

<output>
Create `.planning/phases/04-bonus-features/04-04-SUMMARY.md` when done
</output>
