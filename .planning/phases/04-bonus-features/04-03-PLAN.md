---
phase: 04-bonus-features
plan: 03
type: execute
wave: 2
depends_on: [04-02]
files_modified:
  - src/components/ArchivedStamp.tsx
  - src/app/c/[billId]/page.tsx
  - src/app/dashboard/[billId]/page.tsx
autonomous: true
requirements: [BONUS-03, BONUS-04]

must_haves:
  truths:
    - "Member view (/c/[billId]) shows an ARCHIVED stamp overlay with 'THIS CHIT IS ARCHIVED' sub-copy and detail copy when bill.archivedAt is set; all claim/pay action buttons are disabled"
    - "Dashboard (/dashboard/[billId]) shows a full-width 'BILL ARCHIVED — READ ONLY' banner and hides all confirm/reject/remind action buttons when bill.archivedAt is set"
    - "Each dashboard MemberRow with status 'CLAIMED — UNPAID' has a working SEND REMINDER button that opens wa.me with a Manglish pre-filled message containing the member name, bill title, and share URL"
    - "SEND REMINDER button is NOT shown for AWAITING, CONFIRMED, or N/A members (D-07)"
    - "archivedBill.test.tsx tests pass GREEN after ArchivedStamp.tsx is created"
    - "MemberRow nudge tests (existing MemberRow.test.tsx) remain green"
  artifacts:
    - path: "src/components/ArchivedStamp.tsx"
      provides: "ARCHIVED stamp visual component (Bungee, text-stamp, ink-bleed filter)"
    - path: "src/app/c/[billId]/page.tsx"
      provides: "ARCHIVED overlay check using bill.archivedAt + disabled buttons"
    - path: "src/app/dashboard/[billId]/page.tsx"
      provides: "ARCHIVED banner + frozen action buttons + per-member NUDGE WhatsApp handler"
  key_links:
    - from: "src/app/c/[billId]/page.tsx"
      to: "ArchivedStamp component"
      via: "conditional render when billData.archivedAt is defined"
      pattern: "ArchivedStamp|archivedAt"
    - from: "src/app/dashboard/[billId]/page.tsx"
      to: "NUDGE handler"
      via: "handleNudgeMember(memberName) → window.open(wa.me)"
      pattern: "handleNudgeMember|wa\\.me"
    - from: "src/app/dashboard/[billId]/page.tsx"
      to: "MemberRow onRemind prop"
      via: "conditional: only when status === 'CLAIMED — UNPAID' (D-07)"
      pattern: "CLAIMED.*UNPAID.*handleNudgeMember"
---

<objective>
Implement the user-visible side of BONUS-03 (ARCHIVED stamp on member view and dashboard) and BONUS-04 (per-member WhatsApp nudge on dashboard). These two features share the dashboard page file and are delivered as one vertical slice.

Purpose: After this plan, a user visiting an archived bill sees the ARCHIVED stamp; a dashboard organizer can nudge individual unpaid members via WhatsApp.
Output: ArchivedStamp.tsx (new component), c/[billId]/page.tsx (ARCHIVED overlay + disabled buttons), dashboard/[billId]/page.tsx (ARCHIVED banner + NUDGE handler).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-bonus-features/04-CONTEXT.md
@.planning/phases/04-bonus-features/04-UI-SPEC.md
@.planning/phases/04-bonus-features/04-PATTERNS.md

@.planning/phases/04-bonus-features/04-01-SUMMARY.md
@.planning/phases/04-bonus-features/04-02-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts and patterns the executor needs — extracted from codebase. -->

From src/components/SettleStamp.tsx (ARCHIVED stamp follows this visual contract exactly):
```typescript
// SettleStamp pending state (lines 44-59) — use as ArchivedStamp template
// Key: border-2 border-stamp rounded px-4 py-2, rotate(-6deg), filter: url(#ink-bleed)
// Bungee font rendered via font-stamp font-family variable
// ARCHIVED differences: no opacity-50, no animation, different text
```

From src/app/c/[billId]/page.tsx (member view — key state variables):
```typescript
const bill = useQuery(api.bills.getBillForMember, ...)
// bill is null (not found) | undefined (loading) | { ...billWithoutSecret, items, claims, qrUrl, receiptUrl, archivedAt?: number }
// archivedAt is included in the getBillForMember response (convex/bills.ts line 102 spreads the full bill minus organizerSecret)
// Insert ARCHIVED check after the null/loading guards and BEFORE the main render return
```

From src/app/dashboard/[billId]/page.tsx (key patterns for NUDGE):
```typescript
// shareUrl is set in useEffect (line 31-33): setShareUrl(`${window.location.origin}/c/${billId}`)
// bill.title is available from billData.bill.title (line 120)
// Existing handleRemind() on line 165-169 — REPLACE with per-member handleNudgeMember(memberName: string)
// MemberRow onRemind prop is called with no args (existing interface) — wrap the new handler:
//   onRemind={memberStatus === "CLAIMED — UNPAID" ? () => handleNudgeMember(payment.claimantName) : undefined}
```

From src/app/share/[billId]/page.tsx lines 56-58 (wa.me URL pattern — MATCH EXACTLY):
```typescript
const whatsAppUrl = `https://wa.me/?text=${encodeURIComponent(manglishMessage)}`;
// Use window.open(url, "_blank") instead of anchor tag
```

From src/components/MemberRow.tsx (MemberRow interface — unchanged):
```typescript
type MemberRowProps = Readonly<{
  name: string;
  status: StatusValue;   // "CONFIRMED" | "AWAITING" | "CLAIMED — UNPAID" | "UNCLAIMED ❋" | "N/A"
  amountOwed: number;    // integer cents
  onConfirm?: () => void;
  onReject?: () => void;
  onRemind?: () => void; // MemberRow shows SEND REMINDER button only when this is defined AND status triggers showRemind
}>
```

From 04-UI-SPEC.md (ARCHIVED stamp spec):
```
Font: Bungee 28px UPPERCASE
Text: ARCHIVED
Color: text-stamp border-stamp (#B91C1C)
Transform: rotate(-6deg)
Filter: filter: url(#ink-bleed)
Opacity: 100% (no opacity-50 — archived is a final state)
Sub-copy: "THIS CHIT IS ARCHIVED" — JetBrains Mono 12px UPPERCASE text-stamp tracking-widest
Detail copy: "This chit was automatically archived after 30 days of inactivity." — 12px body text-ink opacity-70
```

From 04-UI-SPEC.md (Dashboard ARCHIVED state):
```
Archive banner: full-width bg-stamp text-white banner: "BILL ARCHIVED — READ ONLY"
Font: JetBrains Mono 12px bold UPPERCASE tracking-widest
Buttons: hidden (do not pass onConfirm/onReject/onRemind props to MemberRow when archived)
```

From 04-UI-SPEC.md (Nudge message):
```
"Eh {memberName}, still haven't paid for the {billTitle} chit lah! Tap here to settle: {shareUrl}"
encodeURIComponent before inserting in wa.me URL
```

Security note (T-04-04): memberName (claimantName) is already stored in Convex and retrieved via getPaymentsForBill — it is not raw user input at this point. However, to mitigate wa.me URL injection, apply `.replace(/[<>"]/g, '')` on memberName before interpolation into the WhatsApp message string (defense in depth — claimantName is stored user input from a previous session).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create ArchivedStamp component + wire into member view</name>
  <files>src/components/ArchivedStamp.tsx, src/app/c/[billId]/page.tsx</files>
  <read_first>
    - src/components/SettleStamp.tsx — read in full; the ArchivedStamp follows the exact same visual structure as the pending-state SettleStamp; copy the div/span pattern and adapt it
    - src/app/c/[billId]/page.tsx — read in full; locate the null/loading guards and the main return statement; identify where to add the ARCHIVED overlay check and where to apply disabled={!!billData?.archivedAt} to claim and pay buttons
    - .planning/phases/04-bonus-features/04-UI-SPEC.md — read "ARCHIVED State" → "Member view" section and "ARCHIVED Stamp" surface treatment for exact values
    - .planning/phases/04-bonus-features/04-PATTERNS.md — read "src/app/c/[billId]/page.tsx — ARCHIVED stamp" section
  </read_first>
  <action>
    Two changes:

    1. Create src/components/ArchivedStamp.tsx. This is a named export `ArchivedStamp`, no props needed — it is a pure display component. Mark `"use client"` (consistent with other stamp components). Structure: centered div with three elements:
       - stamp box: `inline-block border-2 border-stamp rounded px-4 py-2` with `style={{ transform: "rotate(-6deg)", filter: "url(#ink-bleed)" }}`; inner span: Bungee font `text-3xl font-bold text-stamp uppercase tracking-widest` text "ARCHIVED"
       - sub-copy paragraph: `text-xs text-stamp uppercase tracking-widest mt-1` text "THIS CHIT IS ARCHIVED"
       - detail copy paragraph: `text-xs text-ink mt-2` with `style={{ opacity: 0.7 }}` text "This chit was automatically archived after 30 days of inactivity."
       Use `font-[family-name:var(--font-stamp)]` for the stamp text (matches pattern from other pages). Wrap everything in `<div className="text-center my-6">`.

    2. Modify src/app/c/[billId]/page.tsx:
       - Add import: `import { ArchivedStamp } from "@/components/ArchivedStamp"` alongside other component imports
       - After the `if (bill === null)` return block and before the main return: add a conditional block. When `bill?.archivedAt` is defined (truthy), render a full-page centered view with the ArchivedStamp component. Pattern: `<main className="min-h-screen bg-paper-table flex items-center justify-center"><div className="max-w-[480px] mx-auto px-4 py-12"><ArchivedStamp /></div></main>`. Return early — do not fall through to the main bill render.
       - For the "I'VE PAID" button: add `disabled={!!bill?.archivedAt}` to the button element. Existing `disabled` logic already exists for `isPaying` — combine with `disabled={isPaying || !!bill?.archivedAt}`.
       - For each item row tap handler, add a guard: `if (bill?.archivedAt) return` at the top of handleItemTap before the existing `if (!claimantSession) return` check.
  </action>
  <verify>
    <automated>cd /Users/irfanmurad/Developer/vessl/tongtong-bill-splitting && pnpm test -- archivedBill 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ArchivedStamp.tsx exists with named export `ArchivedStamp`
    - ArchivedStamp renders "ARCHIVED" in a stamp box with `border-stamp`, `text-stamp`, `rotate(-6deg)`, `filter: url(#ink-bleed)`
    - ArchivedStamp renders "THIS CHIT IS ARCHIVED" sub-copy
    - ArchivedStamp renders "This chit was automatically archived after 30 days of inactivity." detail copy
    - src/app/c/[billId]/page.tsx imports ArchivedStamp
    - Member view returns ArchivedStamp early when `bill?.archivedAt` is truthy
    - `pnpm test -- archivedBill` exits 0 (previously RED tests now GREEN)
    - `pnpm test` shows all tests pass (no regressions)
  </acceptance_criteria>
  <done>ArchivedStamp component created; member view shows ARCHIVED overlay for archived bills; archivedBill tests green</done>
</task>

<task type="auto">
  <name>Task 2: Wire ARCHIVED banner + per-member NUDGE into dashboard</name>
  <files>src/app/dashboard/[billId]/page.tsx</files>
  <read_first>
    - src/app/dashboard/[billId]/page.tsx — read in full; locate: the existing handleRemind() function at line ~165-169; the MemberRow mapping block at lines ~300-320 where onRemind={handleRemind} is passed; the area after the bill title/displayCode heading to insert the archive banner
    - src/app/share/[billId]/page.tsx — read lines 56-58 for the wa.me URL construction pattern to match exactly
    - .planning/phases/04-bonus-features/04-UI-SPEC.md — read "Reminder Nudge" section for WhatsApp message exact copy and sanitization note; read "Dashboard ARCHIVED state" for banner spec
    - .planning/phases/04-bonus-features/04-PATTERNS.md — read "src/app/dashboard/[billId]/page.tsx" pattern section
    - src/components/MemberRow.tsx — confirm onRemind?: () => void interface (no changes needed to MemberRow)
  </read_first>
  <action>
    Three edits to src/app/dashboard/[billId]/page.tsx:

    1. Replace handleRemind() with handleNudgeMember(memberName: string):
       - Remove the existing `handleRemind` function (lines ~165-169)
       - Add a new function: `function handleNudgeMember(memberName: string) { const sanitizedName = memberName.replace(/[<>"]/g, ''); const msg = encodeURIComponent(\`Eh \${sanitizedName}, still haven't paid for the \${bill.title} chit lah! Tap here to settle: \${shareUrl}\`); window.open(\`https://wa.me/?text=\${msg}\`, "_blank"); }`
       - This function uses `bill.title` (already in scope from `const { bill, items } = billData` on line ~120) and `shareUrl` (already in scope from useState).

    2. Update MemberRow mapping (lines ~300-320):
       - Change `onRemind={handleRemind}` to: `onRemind={memberStatus === "CLAIMED — UNPAID" ? () => handleNudgeMember(payment.claimantName) : undefined}`
       - This change implements D-07: NUDGE only for "CLAIMED — UNPAID" status. The MemberRow.tsx showRemind condition already gates the button display on status; this change additionally ensures we pass undefined for all other statuses so the button is not shown for AWAITING/CONFIRMED/N/A members.
       - When `bill.archivedAt` is set (archived), pass `onConfirm={undefined}` and `onReject={undefined}` and `onRemind={undefined}` to ALL MemberRow instances — remove all action callbacks. Implement this by deriving a boolean `const isArchived = !!bill.archivedAt` after line ~120, then using it in the MemberRow mapping: `onConfirm={isArchived ? undefined : () => handleConfirm(payment._id)}`, `onReject={isArchived ? undefined : () => handleReject(payment._id)}`, `onRemind={isArchived ? undefined : (memberStatus === "CLAIMED — UNPAID" ? () => handleNudgeMember(payment.claimantName) : undefined)}`.

    3. Add ARCHIVED banner:
       - After the `<h1>` title and `<p>` displayCode heading block (around line ~249-252), before the 2-column grid div — add: `{isArchived && (<div className="w-full bg-stamp text-white text-xs font-bold uppercase tracking-widest py-3 text-center mb-4">BILL ARCHIVED — READ ONLY</div>)}`.
  </action>
  <verify>
    <automated>cd /Users/irfanmurad/Developer/vessl/tongtong-bill-splitting && pnpm test -- MemberRow 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/dashboard/[billId]/page.tsx no longer contains `handleRemind` function
    - File contains `handleNudgeMember` function with `memberName.replace(/[<>"]/g, '')` sanitization (T-04-04)
    - File contains `window.open(\`https://wa.me/?text=\${msg}\`, "_blank")` using encodeURIComponent
    - MemberRow onRemind prop is passed as `undefined` for all statuses except "CLAIMED — UNPAID"
    - When `isArchived` is true, all MemberRow action props (onConfirm, onReject, onRemind) are undefined
    - ARCHIVED banner div contains "BILL ARCHIVED — READ ONLY" text and `bg-stamp` class
    - `pnpm test -- MemberRow` exits 0 (existing MemberRow tests still pass)
    - `pnpm test` shows full suite green
  </acceptance_criteria>
  <done>Dashboard has per-member NUDGE WhatsApp handler; ARCHIVED banner and frozen action buttons implemented; full test suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stored claimantName → wa.me URL | claimantName is stored user input from a prior session; sanitize before interpolation into WhatsApp deep link |
| bill.archivedAt → UI state | read from Convex (trusted source); no user input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | wa.me URL construction in handleNudgeMember | mitigate | Apply `.replace(/[<>"]/g, '')` on claimantName before interpolation into the WhatsApp message string; message is encodeURIComponent-wrapped before insertion in the URL |
| T-04-W2-01 | Tampering | ArchivedStamp — client renders but mutations already blocked server-side | accept | UI disables buttons (defense in depth); server-side freeze check in plan 02 is the authoritative gate; dual protection |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — all components use existing Tailwind tokens and React |
</threat_model>

<verification>
`pnpm test -- archivedBill` exits 0 — ArchivedStamp tests GREEN
`pnpm test -- MemberRow` exits 0 — existing MemberRow tests unaffected
`pnpm test` — full suite green, no regressions
grep -n "handleNudgeMember" src/app/dashboard/[billId]/page.tsx — appears in function definition and MemberRow mapping
grep -n "handleRemind" src/app/dashboard/[billId]/page.tsx — should return 0 matches (removed)
grep -n "BILL ARCHIVED" src/app/dashboard/[billId]/page.tsx — appears in banner div
grep -n "ArchivedStamp" src/app/c/[billId]/page.tsx — appears in import and conditional render
</verification>

<success_criteria>
- ArchivedStamp.tsx created with correct visual contract (Bungee, text-stamp, rotate(-6deg), ink-bleed, no opacity reduction)
- Member view shows full-page ARCHIVED overlay when bill.archivedAt is truthy — all action buttons disabled
- Dashboard shows full-width stamp-red banner "BILL ARCHIVED — READ ONLY" when isArchived
- Dashboard all MemberRow actions hidden when isArchived
- Per-member NUDGE opens wa.me with Manglish message containing member name (sanitized), bill title, and share URL
- NUDGE only shows for "CLAIMED — UNPAID" members (D-07)
- T-04-04: claimantName sanitized with .replace(/[<>"]/g, '') before URL construction
- Full test suite green
</success_criteria>

<output>
Create `.planning/phases/04-bonus-features/04-03-SUMMARY.md` when done
</output>
