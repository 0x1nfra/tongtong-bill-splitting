---
phase: 01-working-bill
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/app/create/page.tsx
  - src/components/ItemRow.tsx
  - src/components/RunningTotal.tsx
autonomous: true
requirements:
  - BILL-01
  - BILL-02
  - BILL-03
  - BILL-05
  - BILL-06
  - AUTH-01

must_haves:
  truths:
    - "Organizer can enter bill title, venue/restaurant name, date, and add multiple item rows with name, price (RM), quantity"
    - "Items are editable and deletable inline after being added"
    - "SST 6% and service charge 10% toggles update the running total display live"
    - "'GENERATE LINK' button is disabled (opacity-50 cursor-not-allowed) when items array is empty"
    - "All items live in React local state — no Convex calls until 'GENERATE LINK' is tapped"
    - "Running totals show subtotal / service charge / SST / grand total with RM prefix and 2dp format"
  artifacts:
    - path: "src/app/create/page.tsx"
      provides: "Full bill builder form — title field, venue name field, date field, item list, toggles, running totals, GENERATE LINK button calling createBill"
    - path: "src/components/ItemRow.tsx"
      provides: "Single item row with name/price/qty inputs in edit mode and display row, inline × delete button"
    - path: "src/components/RunningTotal.tsx"
      provides: "Live subtotal/tax/total display component derived from items array + toggle state"
  key_links:
    - from: "src/app/create/page.tsx"
      to: "convex/bills.ts createBill"
      via: "useMutation(api.bills.createBill) called on GENERATE LINK tap"
      pattern: "api\\.bills\\.createBill"
    - from: "src/app/create/page.tsx"
      to: "next/navigation useRouter"
      via: "router.push(`/share/${billId}`) after createBill resolves"
      pattern: "router\\.push.*share"
    - from: "src/components/RunningTotal.tsx"
      to: "src/app/create/page.tsx"
      via: "items prop + applySST + applyServiceCharge props"
      pattern: "applySST.*applyServiceCharge"
---

## Phase Goal

**As an** organizer, **I want to** create a bill and share it with friends, **so that** everyone can see what they owe and confirm payment without me chasing them.

<objective>
Complete the bill builder form at /create — the primary organizer entry point. After this plan, an organizer can fill in a real bill with title, venue name, date, items and tax options, tap "GENERATE LINK →", and be redirected to /share/[billId] with the bill persisted in Convex.

Purpose: This is the first complete vertical slice — a real user action writes real data to the DB and produces a real shareable URL.
Output: Full /create page with ItemRow and RunningTotal components, createBill mutation wired to "GENERATE LINK →" button, redirect to /share/[billId] on success.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-working-bill/01-CONTEXT.md
@.planning/phases/01-working-bill/01-RESEARCH.md
@.planning/phases/01-working-bill/01-UI-SPEC.md
@.planning/phases/01-working-bill/01-02-SUMMARY.md

<interfaces>
<!-- Types the executor needs — define these in create/page.tsx and pass to components -->

Item type (local state only — NOT a Convex type):
  type ItemDraft = {
    id: string          // crypto.randomUUID() for React key
    name: string
    price: string       // raw RM string input: "12.50" — converted to cents on submit
    quantity: number
  }

ItemRow props:
  type ItemRowProps = Readonly<{
    item: ItemDraft
    onUpdate: (id: string, field: keyof ItemDraft, value: string | number) => void
    onDelete: (id: string) => void
  }>

RunningTotal props:
  type RunningTotalProps = Readonly<{
    items: ItemDraft[]
    applySST: boolean
    applyServiceCharge: boolean
  }>

calculateTotals function (from RESEARCH.md Pattern 7):
  Input: items (ItemDraft[]), applySST, applyServiceCharge
  Logic: subtotal = sum(Math.round(parseFloat(item.price || "0") * 100) * item.quantity)
  serviceCharge = applyServiceCharge ? Math.round(subtotal * 0.10) : 0
  afterSC = subtotal + serviceCharge
  sst = applySST ? Math.round(afterSC * 0.06) : 0
  grandTotal = afterSC + sst
  Display: (cents / 100).toFixed(2) with "RM" prefix, no space

createBill mutation args (from convex/bills.ts — updated in Plan 01):
  organizerSecret: string
  title: string
  applySST: boolean
  applyServiceCharge: boolean
  qrStorageId?: Id<"_storage">   // not wired in this plan (Plan 04)
  venueName?: string             // optional restaurant/venue name (BILL-01)
  billDate?: string              // optional ISO date string "YYYY-MM-DD" (BILL-01)
  items: Array<{ name: string, price: number, quantity: number, orderIndex: number }>
  Note: price must be Math.round(parseFloat(item.price) * 100) — integer cents

Color and layout constraints (D-11, D-12, D-13 / UI-SPEC):
  - "GENERATE LINK →" button: full-width h-12 (48px) bg-[--color-pen] text-white uppercase font-bold
  - Disabled state (no items): opacity-50 cursor-not-allowed pointer-events-none
  - Item row layout: flex row — name input (flex-1) / price input (w-20) / qty input (w-12) / × button (44×44px min)
  - × button: text-[--color-ink] NOT text-[--color-stamp] (red is only for SETTLE stamp and ❋)
  - Toggle: HTML checkbox or button toggle — label "SST 6%" and "SERVICE CHARGE 10%"
  - Container: max-w-[480px] mx-auto px-4 py-8
  - All section headings uppercase
  - "ADD ITEM" button: full-width h-11 border border-[--color-ink] uppercase — neutral, not blue (blue reserved for primary CTAs only)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ItemRow and RunningTotal components</name>
  <files>src/components/ItemRow.tsx, src/components/RunningTotal.tsx</files>
  <read_first>
    - .planning/phases/01-working-bill/01-UI-SPEC.md — Component Inventory, Layout Contracts (Bill Builder), color constraints
    - .planning/phases/01-working-bill/01-RESEARCH.md — Pattern 7 (calculateTotals function), Project Constraints list
    - src/app/globals.css — confirm color token names exactly
  </read_first>
  <action>
    Create src/components/ItemRow.tsx:
    - "use client" directive
    - Export named function ItemRow (not default)
    - Props: Readonly<{ item: ItemDraft, onUpdate: (id, field, value) => void, onDelete: (id) => void }>
    - Define ItemDraft type in this file and re-export it: export type ItemDraft = { id: string; name: string; price: string; quantity: number }
    - Layout: flex row with gap-2, min-height 48px for comfortable tap ergonomics
    - name input: flex-1, type="text", placeholder="Item name", value=item.name, onChange calls onUpdate(item.id, "name", e.target.value)
    - price input: w-20, type="text", inputMode="decimal", placeholder="0.00", value=item.price, onChange calls onUpdate(item.id, "price", e.target.value). Prefix: "RM" label inline before input.
    - quantity input: w-12, type="number", min="1", value=item.quantity, onChange calls onUpdate(item.id, "quantity", parseInt(e.target.value) || 1)
    - × delete button: min 44×44px touch target, text "×", text-[--color-ink] (NOT red — red reserved for stamp/❋ only), onClick calls onDelete(item.id)
    - All inputs: border border-[--color-ink] rounded px-2 py-1 bg-[--color-paper-chit] text-[--color-ink] text-sm

    Create src/components/RunningTotal.tsx:
    - "use client" directive
    - Export named function RunningTotal (not default)
    - Import ItemDraft type from "./ItemRow"
    - Props: Readonly<{ items: ItemDraft[], applySST: boolean, applyServiceCharge: boolean }>
    - Implement calculateTotals inline (or as a module-level helper):
      subtotal = items.reduce((sum, item) => sum + Math.round(parseFloat(item.price || "0") * 100) * item.quantity, 0)
      serviceCharge = applyServiceCharge ? Math.round(subtotal * 0.10) : 0
      afterSC = subtotal + serviceCharge
      sst = applySST ? Math.round(afterSC * 0.06) : 0
      grandTotal = afterSC + sst
    - Render rows: "SUBTOTAL" / "SERVICE CHARGE (10%)" (only if applyServiceCharge) / "SST (6%)" (only if applySST) / "TOTAL" — each row is two columns: label left, amount right
    - Amounts display: "RM" + (cents / 100).toFixed(2) — never raw integers
    - "TOTAL" row: font-bold, larger text (text-lg)
    - Container: border-t border-[--color-ink] pt-4 mt-4 space-y-2
  </action>
  <verify>
    <automated>cd /Users/irfanmurad/Developer/vessl/tongtong-bill-splitting && npx tsc --noEmit 2>&1 | grep -E "src/components/ItemRow|src/components/RunningTotal" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ItemRow.tsx exports ItemDraft type and ItemRow function (both named exports)
    - src/components/ItemRow.tsx contains inputMode="decimal" on price input (mobile keyboard optimization)
    - src/components/ItemRow.tsx × button does NOT use text-[--color-stamp] or color "#B91C1C" (red reserved for stamp/❋)
    - src/components/RunningTotal.tsx contains Math.round(parseFloat( for subtotal calculation (integer cents)
    - src/components/RunningTotal.tsx displays amounts with (cents / 100).toFixed(2) format
    - src/components/RunningTotal.tsx contains "RM" prefix in displayed amounts
    - No TypeScript errors in either component file
  </acceptance_criteria>
  <done>ItemRow and RunningTotal components exist, typed, and compile without errors</done>
</task>

<task type="auto">
  <name>Task 2: Complete /create page — bill details, items state, toggles, createBill mutation, redirect</name>
  <files>src/app/create/page.tsx</files>
  <read_first>
    - src/app/create/page.tsx — current shell from Plan 02 to understand what exists
    - src/components/ItemRow.tsx (just created) — ItemDraft type and ItemRow component to import
    - src/components/RunningTotal.tsx (just created) — RunningTotal component to import
    - .planning/phases/01-working-bill/01-RESEARCH.md — Pattern 8 (router.push after mutation), Anti-Patterns list (router.push from next/navigation), Pattern 3 (createBill mutation args)
    - .planning/phases/01-working-bill/01-UI-SPEC.md — Interaction Contracts (Add item, Edit item, Delete item, Generate Link), Copywriting (CTA labels), Error States (no-items validation)
  </read_first>
  <action>
    Replace src/app/create/page.tsx shell with full bill builder:

    Imports: use "use client"; import useState, useEffect, useCallback from "react"; import useRouter from "next/navigation"; import useMutation from "convex/react"; import api from "@/convex/_generated/api"; import ItemDraft and ItemRow from "@/components/ItemRow"; import RunningTotal from "@/components/RunningTotal".

    State:
    - organizerSecret: string | null — initialized null, loaded in useEffect from localStorage "tongtong_organizer_secret" (generate UUID if missing per AUTH-01)
    - title: string — bill title input
    - venueName: string — restaurant/venue name input (optional, may be empty string on submit)
    - billDate: string — date input value, type="date" (optional, may be empty string on submit)
    - items: ItemDraft[] — empty array initially
    - applySST: boolean — false default
    - applyServiceCharge: boolean — false default
    - isSubmitting: boolean — prevents double-tap

    Handlers:
    - addItem: appends new ItemDraft with crypto.randomUUID() id, empty name, empty price, quantity 1
    - updateItem: replaces item in array by id with new field value
    - deleteItem: filters item from array by id
    - handleGenerate: async — guards: if !organizerSecret return (null check per RESEARCH.md anti-patterns); if items.length === 0 return (BILL-06). Calls createBill mutation with organizerSecret, title, applySST, applyServiceCharge, qrStorageId: undefined (Plan 04 adds QR), venueName: venueName || undefined (pass undefined rather than empty string when blank), billDate: billDate || undefined (same), items mapped to { name, price: Math.round(parseFloat(i.price || "0") * 100), quantity: i.quantity, orderIndex: index }. Awaits result (billId), then calls router.push(`/share/${billId}`) per D-02. Import router from next/navigation, NOT next/router.

    Layout (max-w-[480px] mx-auto px-4 py-8):
    - Heading "CREATE NEW CHIT" uppercase text-2xl font-bold mb-6
    - Bill details section (all fields grouped before items):
      - Bill title input: label "BILL TITLE" uppercase text-xs, input type="text" full-width border border-[--color-ink] bg-[--color-paper-chit] px-3 py-2 text-[--color-ink]
      - Venue name input: label "RESTAURANT / VENUE" uppercase text-xs, input type="text" full-width border border-[--color-ink] bg-[--color-paper-chit] px-3 py-2 text-[--color-ink], placeholder="e.g. Pak Mat Nasi Lemak" (optional — no asterisk or required marker)
      - Date input: label "DATE" uppercase text-xs, input type="date" full-width border border-[--color-ink] bg-[--color-paper-chit] px-3 py-2 text-[--color-ink] (optional)
    - Items section heading "ITEMS" uppercase text-sm font-bold mt-6 mb-2
    - Map items to ItemRow components (key=item.id)
    - If items.length === 0: show empty state "ADD YOUR FIRST ITEM" in text-[--color-ink] opacity-60 text-sm text-center py-4
    - "ADD ITEM" button: full-width h-11 border border-[--color-ink] text-[--color-ink] uppercase text-sm — neutral styling (NOT blue — blue reserved for primary CTAs only per UI-SPEC)
    - Tax toggles section: two rows, each with label + checkbox. "SERVICE CHARGE (10%)" and "SST (6%)" — standard HTML checkboxes
    - RunningTotal component: pass items, applySST, applyServiceCharge
    - "GENERATE LINK →" button: full-width h-12 bg-[--color-pen] text-white uppercase font-bold text-base. Disabled when items.length === 0 OR isSubmitting — apply opacity-50 cursor-not-allowed. Per BILL-06: items.length === 0 makes button non-interactive (add disabled attribute AND the Tailwind classes).
    - Below button if items.length === 0: inline validation message "ADD AT LEAST ONE ITEM — You cannot share an empty chit." text-[--color-ink] opacity-70 text-xs text-center
  </action>
  <verify>
    <automated>cd /Users/irfanmurad/Developer/vessl/tongtong-bill-splitting && npx tsc --noEmit 2>&1 | grep "src/app/create" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - src/app/create/page.tsx contains venueName state and a "RESTAURANT / VENUE" labeled text input (BILL-01)
    - src/app/create/page.tsx contains billDate state and a "DATE" labeled date input type="date" (BILL-01)
    - src/app/create/page.tsx passes venueName: venueName || undefined and billDate: billDate || undefined to createBill (empty strings become undefined)
    - src/app/create/page.tsx contains useMutation(api.bills.createBill)
    - src/app/create/page.tsx contains router.push(`/share/${billId}`) (redirect to share screen per D-02)
    - src/app/create/page.tsx does NOT import from "next/router" (must be "next/navigation")
    - src/app/create/page.tsx contains items.length === 0 check before calling createBill (BILL-06 validation)
    - src/app/create/page.tsx contains Math.round(parseFloat( for price-to-cents conversion on submit
    - src/app/create/page.tsx contains disabled attribute on GENERATE LINK button when items.length === 0
    - "ADD ITEM" button does NOT use bg-[--color-pen] or #1E40AF (blue is reserved for primary CTAs per UI-SPEC)
    - No TypeScript errors in src/app/create/page.tsx
  </acceptance_criteria>
  <done>Organizer can fill in title, venue name (optional), date (optional), items, and toggles on /create, tap GENERATE LINK, bill is created in Convex with all fields, browser redirects to /share/[billId]</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form→createBill mutation | Title, venue name, and item name/price/qty are free-text user input; no server-side sanitization in Convex mutations (Convex v.string() validates type only) |
| localStorage→createBill | organizerSecret passed from localStorage — null-guarded before mutation call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Item price input — float vs int | mitigate | Math.round(parseFloat(price) * 100) converts to integer cents before sending to Convex; display always uses (cents/100).toFixed(2) |
| T-03-02 | Denial of Service | Organizer adds 1000+ items | accept | Demo scale; Convex free tier sufficient; no item-count limit needed for MVP |
| T-03-03 | Spoofing | organizerSecret null on first render causes silent no-op | mitigate | Guard: if (!organizerSecret) return in handleGenerate — button is active but mutation is blocked until localStorage hydrates |
| T-03-04 | Tampering | createBill called with empty items array | mitigate | Client-side guard items.length === 0 prevents handleGenerate from calling mutation (BILL-06) |
| T-03-SC | Tampering | npm installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
Manual verification after tasks complete:

1. Visit http://localhost:3000/create
2. Enter a bill title
3. Enter a venue/restaurant name (optional field — verify it accepts input and can be left blank)
4. Select a date (optional field — verify it accepts a date and can be left blank)
5. Tap "ADD ITEM" — verify new row appears with name/price/qty inputs
6. Enter item data; verify running total updates live
7. Toggle SST — verify total updates
8. Attempt "GENERATE LINK →" with no items — button should be disabled/greyed
9. Add an item then tap "GENERATE LINK →" — browser should redirect to /share/[billId]
10. Verify the billId in the URL is a real Convex ID (not "undefined")
</verification>

<success_criteria>
- ItemRow and RunningTotal components exist and are imported by /create page
- /create page has BILL TITLE, RESTAURANT / VENUE (optional), and DATE (optional) inputs (BILL-01)
- venueName and billDate passed as optional args to createBill (undefined when blank)
- Items managed in React state; no Convex calls during editing (per D-08)
- createBill mutation called only on "GENERATE LINK →" tap
- Price converted to integer cents before mutation (Math.round(parseFloat * 100))
- GENERATE LINK button disabled when items.length === 0 (BILL-06)
- After successful createBill, browser redirects to /share/[billId] (D-02)
- Service charge applied before SST in calculateTotals (Malaysian restaurant convention)
</success_criteria>

<output>
Create `.planning/phases/01-working-bill/01-03-SUMMARY.md` when done
</output>
